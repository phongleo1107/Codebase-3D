# TODO

> **2026-08-31 — scope changed to a 3-day MVP sprint (ADR-011, ADR-012).** Priority order is now: security → the wired end-to-end pipeline → C4/service-map/file-explanation (the three features the sprint exists to deliver) → deploy. Items below are grouped by day, not by theme. Anything not scheduled in Day 1/2/3 is **Deferred**, not cancelled — see the note at the bottom.

> **2026-08-31, later the same day — the LLM layer is cut (ADR-013 supersedes ADR-012).** The three features stay; their implementation becomes deterministic. Descriptions are the file's own leading header comment, route summaries are the comment above the handler, and the diagram is generated from the graph (field renamed `c4` → `componentDiagram`). **`POST /api/explain` and the HMAC token mechanism are out of scope**, so Day 2 loses its entire backend half and Day 1 gains one small extractor. The freed time is deliberately left as buffer — see Day 2's note.

Priority order within each day: security → correctness → core functionality → performance → UX → nice-to-have.

## Done before the sprint (2026-08-29)

- [x] `.gitignore`, `pyproject.toml` (pinned deps, `requires-python >=3.14,<3.15`), `app/config.py`, `app/errors.py`, `app/models/`, `app/logging_setup.py` — contract layer frozen
- [x] `app/security/url_validation.py` + `app/security/net_guard.py` — 389 tests, SSRF/redirect/DNS-rebinding controls mutation-tested
- [x] `app/fetch/github.py` — preflight, validated single redirect, credential-free download request (ADR-009) — 88 tests, 12 controls mutation-tested
- [x] `app/fetch/archive.py` + `app/analysis/deadline.py` — streaming extraction, path/symlink/bomb/multi-root controls — 142 tests, 24 controls mutation-tested
- [x] `app/security/secret_filter.py` + `app/security/path_safety.py` — 142 tests, 18 controls mutation-tested. **No caller yet** — Day 1 wires both in
- [x] `app/analysis/parser.py` — import extraction via tree-sitter — 75 tests, 26 controls mutation-tested. **No caller yet** — Day 1 wires it in
- [x] tree-sitter spike verified: ABI 14, `QueryCursor` present, `progress_callback` unusable (ADR-010)

996 tests total, nothing joined to anything else yet. This was the sprint's starting point; Day 1's first three bullets below have since closed and the modules are joined.

## Day 1 — Backend: wire the pipeline, apply the orphaned security modules, detect routes

- [x] **Wire contract extended for ADR-012** — `ServiceEndpoint`, `AnalyzeResponse.serviceMap`, `AnalyzeResponse.c4`, three `Settings` limits, 17 tests (1013 total). Both fields default to absent, so `/api/analyze` can be built and shipped before the LLM layer exists and still return a valid response

- [x] **Pipeline (`app/analysis/pipeline.py`)** — one `Deadline` per request from `Settings`, `github.download_request()` sent and streamed, **`response.iter_raw()`** into `archive.iter_source_files`, commit SHA harvested from the tar root via the **`ArchiveInfo`** out-parameter (ADR-015). 60 tests plus 9 for the new channel; 33 controls mutation-tested, 32 caught, the survivor annotated. **The first code path that joins two modules.** *(Corrected 2026-08-31: this entry previously said `iter_bytes()` and `ArchiveRoot`, describing PR #3's parallel implementation rather than the code that merged. `iter_raw()` is deliberate — httpx transparently gunzips a `Content-Encoding` response, which would silently change what `MAX_DOWNLOAD_BYTES` and the compression-ratio denominator measure.)*
- [x] **`is_secret_path` applied** in the pipeline, before the bytes reach the parser, with the skips counted. The SECURITY.md secret-exposure row is **still `Partial`** and correctly so: that row describes a filter applied during analysis *and* re-applied independently at the point of serving, and `/api/explain` — the second caller — is Day 2 work. *(ADR-013, later the same day: `/api/explain` no longer exists. The second caller is now `/api/source`, which is post-MVP, so this row will not flip during the sprint. The analysis-time filter described here is unchanged and still correct.)*
- [x] **`extract_imports` called**, grammar picked by extension via the `_BY_EXTENSION` map. `MAX_SOURCE_FILES` enforced after the filters, truncation deterministic and flagged
- [x] **End-to-end smoke check against a real public repo** — `backend/scripts/smoke.py`, deliberately outside the pytest suite so `conftest.py`'s network block stays absolute (ADR-014). `p-limit`, `zustand`, and `ky` analyzed clean (6/50/54 files, 1.5–2.2 s); `RepositoryNotFoundError` and `NoSupportedFilesError` confirmed against real responses. No production code changed
- [ ] Apply `safe_relative_path` wherever a resolved path is used for anything beyond an in-memory dict key. **Still not applicable, and now for a sharper reason** — the resolver exists and resolves paths, but resolution is set membership against `RepositoryAnalysis.files`, so a resolved path is always a path the archive reader already validated and is only ever used as an in-memory key. Nothing touches disk (ADR-003), so there is no base directory to keep it inside of
- [x] Add `.mts` and `.cts` to `pipeline._BY_EXTENSION`. **Done 2026-09-01** — 13 tests (1336 → 1349). Both take the **TypeScript** grammar, not TSX, for the reason `.ts` does: TypeScript reads JSX only in a file named `.tsx`, so `<T>expr` in an `.mts` file is a type assertion and TSX would swallow the rest of the file into an ERROR node. `resolver.EXTENSIONS` gained them in the same edit — TS extensions first, `.cjs` still last so the file-beats-directory-index test keeps pitting the last file candidate against the first directory one — and so did the `.mjs` → `.mts` / `.cjs` → `.cts` rewrites, which map **one** extension each rather than falling back to `.ts`: the extension in the specifier names the module kind the compiler will emit, and only an `.mts` source emits an `.mjs`. The `.mjs` → `.ts` non-rewrite stays pinned by its own test. `pipeline`'s UNSUPPORTED list lost `src/a.mts`/`src/a.cts` and gained `src/a.ets`/`src/a.ts.bak` so widening the set stays deliberate. ARCHITECTURE.md's "Source parsing" and "Dependency extraction / resolution" sections now say eight extensions rather than six
- [x] **Wire contract amended for ADR-013** — `c4` → `componentDiagram` (+ `MAX_C4_CHARS` → `MAX_COMPONENT_DIAGRAM_CHARS`), new `GraphNode.description` + `MAX_DESCRIPTION_CHARS`. **Done** — verified in `app/config.py`, `app/models/api.py`, `app/models/graph.py`; 15 tests reference the renamed fields
- [x] `app/analysis/descriptions.py` — a file's leading header comment (JSDoc `/** */`, `/* */`, or a run of `//`), normalized at extraction and carried on `SourceFile` to `GraphNode.description`. **Not from the tree** — a 4 KiB prefix scan, because a header comment is at byte 0 where JS has no lexical ambiguity to get wrong (ADR-020). Only the *normalizer* is shared with `ServiceEndpoint.summary`; route detection will locate its own comment from the tree it already has
- [x] `app/analysis/resolver.py` — **MVP scope: relative imports + bare-specifier-as-external only.** No `tsconfig.json` `paths`, no `baseUrl`, no workspace packages (Deferred — see bottom). Consumes `pipeline.RepositoryAnalysis`: a tuple of content-free `SourceFile` records, each carrying its `ImportRef`s with specifiers exactly as written. Resolution is set-membership against that same collection (ADR-016), so it cannot produce an edge with no node on the far end. **Done 2026-08-31** — 74 tests (1085 → 1159), 23 controls mutation-tested with 22 caught and the survivor annotated. Output is one `ResolvedImport` per `ImportRef` and the config seam is decided but not built (ADR-017). **Nothing calls it yet** — the graph builder is its first caller
- [x] `app/analysis/graph_builder.py` — nodes/edges per the existing model contract; deterministic sort; `stats.dependencies == len(edges)`. **Done 2026-08-31** — 36 tests (1159 → 1195), 22 controls mutation-tested with 21 caught and the survivor annotated. **The resolver's first caller.** Shape decisions recorded in ADR-018: node `id` *is* the path, the repository root is a node (`"."`, `parent: None`, named for the repo), directories are inferred from the parent hierarchy, and ordering is by path *components* rather than by path string. A file/directory path collision and a repeated path — both legal in a tarball — are resolved rather than raised
- [x] `MAX_IMPORTS` — cap the total import count in the pipeline. **Done 2026-08-31** — 9 tests (1195 → 1204), 6 controls mutation-tested with all 6 caught. Closes the import half of SECURITY.md's "Post-parse analysis runs outside the deadline": `resolve_imports` runs with no `Deadline` after the 60 s budget is spent, and nothing capped imports *per file*, so 1 002 000 imports cost 76.1 s to resolve — now 7.7 s. Recorded in ADR-019, including why a count beats a clock here. **The graph-size half is still open** — see `MAX_NODES`/`MAX_EDGES` below
- [x] **`MAX_NODES` / `MAX_EDGES` enforced.** **Done 2026-09-01** — by taking ADR-018's *second* branch: `build_graph` gained an optional `GraphLimits` and builds a smaller graph, rather than the router slicing a large one. Nodes are cut to a prefix of component order (which is closed under `parent`, so nothing is orphaned), edges naming a dropped node go with it, the edge cap applies next, and every counter is derived afterwards — so `stats.dependencies == len(edges)`, the per-node counters, `stats.files`/`directories`, both import totals and every directory aggregate all still describe the graph in the response. Reasoning in ADR-023; 11 cap tests in `tests/test_graph_builder.py` plus 4 over a whole response in `tests/test_api_routes.py`
- [x] `app/analysis/routes.py` — route-detection queries for the service map. **Done 2026-08-31** — 59 tests plus 10 in the pipeline (1267 → 1336), 30 controls mutation-tested with 25 caught and all 5 survivors equivalent-by-construction and annotated. Ships **Express-style method calls** (`app.get/post/put/patch/delete/head/options/all`, any receiver — covers Express, Koa-router, Fastify, Hono) and the **Next.js App Router file convention** (`app/**/route.ts` exporting `GET`/`POST`/…). **Decorator-style routers (NestJS) are deferred** — see below — as is the Next.js Pages Router, whose default-export handler declares no method. Required a seam: `parser.py` now splits into `parse_source` (every guard, one tree) and `extract_imports` (the query), so route detection reads the tree the pipeline already built instead of parsing a second time and re-implementing five security guards (ADR-021). `ServiceEndpoint.summary` is the comment above the handler, via `descriptions.normalize_comment` as ADR-020 predicted
- [x] `app/analysis/component_diagram.py` — deterministic Mermaid from the finished graph: top-level directories as containers, external packages as external systems, detected routes as the API surface. Pure function of the graph, so it is golden-file testable. **Done 2026-09-01** — 46 tests (1336 → 1382), including `tests/fixtures/component_diagram_golden.mmd` compared byte for byte; 29 controls mutation-tested with 27 caught and both survivors equivalent-by-construction and annotated. Decisions recorded in ADR-024: the external system is **one unnamed box with a count**, because ADR-005 kept package names out of the graph and this module is deliberately not given the resolver's specifiers; node identifiers are **synthetic** (`c0`, `r0`, `ext`) so repository text has no position in the output except inside a quoted label; and `MAX_COMPONENT_DIAGRAM_CHARS` is enforced **while writing** — it finally has a producer
- [x] `app/api/` routes: `POST /api/analyze`, `GET /api/health`. **Done 2026-09-01** — `app/api/{routes,middleware,app}.py` plus `app/main.py`, 35 tests (1336 → 1371), 19 controls mutation-tested with all 19 caught. **Four** handlers, not one: `AppError`, `RequestValidationError` → bare `INVALID_REQUEST` (the control — FastAPI's default returns pydantic's `detail`, which embeds the offending input), Starlette's own `HTTPException` (a 404 or a 405 would otherwise be the only responses in the service not matching the contracted body), and a catch-all 500. Also lands the **request-body cap** — `MAX_REQUEST_BODY_BYTES` had never been read — checking `Content-Length` *and* counting delivered bytes, refusing before the application runs. The analysis runs on a worker thread; there is deliberately **no `asyncio.wait_for`** around it (ADR-023). `componentDiagram` is `None` with a TODO until the generator below exists
- [x] Golden-file test over a whole `AnalyzeResponse` — newly possible under ADR-013, since with no LLM in the path the same commit must produce byte-identical JSON. **Done 2026-09-01** — 2 tests (1431 → 1433): `test_the_whole_response_matches_the_golden_file` compares a pretty-printed `response.json()` against the new `tests/fixtures/analyze_response_golden.json` byte for byte, reusing `REALISTIC`/`serve()` already in the file; `test_the_same_commit_produces_byte_identical_json` calls the endpoint twice against the same mocked repository and diffs the raw response text, which is what actually pins "byte-identical" rather than just "matches one fixture"

## Day 2 — Frontend

> ADR-013 removed this day's entire backend half (`app/llm/`, `/api/explain`, prompt guardrails, HMAC issuance). **The freed time is buffer, not a slot to fill** — Day 1 is now large, and no archive byte has ever been fetched over a real network, so that risk is still unmeasured. Do not pull Deferred items forward until Day 1 is green end to end.
>
> **2026-09-01 — the first three bullets are done, and Day 1 is no longer what gates the rest.** The scaffold renders a hand-written fixture because it has no API client; `POST /api/analyze` landed the same day and returns a full response, `componentDiagram` included. So the next bullet is the highest-value one on the board: writing the client is what first drives this code with real output, and it is also the first real test of the hand-transcribed zod schema.
>
> **2026-09-01 (ADR-022, supersedes ADR-002/ADR-004):** the graph renders in 2D via Cytoscape.js, not a custom Three.js/React Three Fiber scene, and layout uses one of Cytoscape's built-in algorithms rather than the sphere-packing worker. The bullet below is updated; everything else on this day is unaffected.

- [x] Scaffold `frontend/` (Vite 8, React 19, TypeScript 5.9.3, Tailwind v4, Zustand 5, zod 4, Cytoscape.js 3.34). **Done 2026-09-01** — all versions exact-pinned and verified on npm before writing, `package-lock.json` committed, `npm run build` (`tsc --noEmit && vite build`) green under `strict` + `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess`. `@types/cytoscape` was installed and removed: npm deprecates it as a stub because Cytoscape ships its own types
- [x] Frontend types + zod schema for `AnalyzeResponse`, incl. `serviceMap`, `componentDiagram`, and the optional `node.description`. **Done 2026-09-01** — `src/api/schema.ts`, a field-for-field transcription of `app/models/graph.py` + `api.py`, with the `Settings` bounds mirrored in `src/api/limits.ts`. **Never validated a server response** — only `src/graph/fixture.ts`, written by the same hand in the same sitting, so a transcription error would be invisible in both. Treat the first real `/api/analyze` call as this schema's first test
- [x] 2D Cytoscape.js graph: nodes/edges from the response, directory hierarchy as compound nodes (backs collapse/expand), one of Cytoscape's built-in layouts. **Done 2026-09-01 at scaffold scope** — `src/scene/GraphCanvas.tsx` + `style.ts`, rendering `src/graph/fixture.ts` because there is no endpoint yet. `GraphNode.parent` (ADR-006) becomes `data.parent` with no transform beyond a rename, which is the whole of the compound hierarchy. Click selects and traces (`selected` / `neighbor` / `faded`). **Layout is still open**: built-in `cose` ships as the placeholder because it needs no extra dependency, and the `cola`/`elk`/`dagre` choice wants a real repository to measure against — see the bullet below
- [ ] **Decide the Cytoscape layout.** `cose` renders very wide, grid-like boxes on nested compounds. `cola`, `elk`, and `dagre` are each a separate pinned dependency, so this is a dependency decision and wants measuring against a real repository's node/edge counts, not the ten-node fixture
- [ ] **Collapse/expand.** Compound nodes back it (ADR-022) but nothing invokes it yet — there is no action, no aggregate node, and no store state for which directories are collapsed
- [x] Inspector panel: click node → `description` (already present in the response — no second request, no loading state, no client cache) + imports/importedBy. **Done 2026-09-01** — `src/ui/Inspector.tsx`. No source preview: `/api/source` is deferred post-MVP with the whole HMAC mechanism (ADR-007, ADR-013)
- [x] Landing page + URL input + loading state, and the `POST /api/analyze` client in `src/api/`. **Done 2026-09-01** — `src/api/client.ts` (`analyzeRepository`, `ApiRequestError`), a landing page + loading/error state in `src/ui/App.tsx`, and a `vite.config.ts` dev-server proxy so the browser's request stays same-origin (the backend has no CORS yet — Day 3). Verified against a live `uvicorn` backend over three real repositories: `AnalyzeResponseSchema` validated the real response unmodified, and a bad URL round-tripped through `ApiErrorSchema` into a real `ApiRequestError`. Neither schema needed a field changed — see `docs/CURRENT_STATE.md`'s Recently Completed entry
- [x] Component diagram panel: render `componentDiagram` Mermaid source via the `mermaid` package. **Done 2026-09-01** — `frontend/src/ui/ComponentDiagram.tsx`, `mermaid` **11.17.2** exact-pinned (verified on npm first; also `latest`), decisions in ADR-025. Config that is control rather than styling: `securityLevel: 'strict'` set explicitly because it gates mermaid's whole-SVG DOMPurify pass, `htmlLabels: false` at the **top level** (the `flowchart.htmlLabels` form is deprecated at this version and warns), `suppressErrorRendering: true` so mermaid does not draw its own error diagram into the document, `startOnLoad: false`. `mermaid.render()` returns an SVG *string*, so the source is **adopted, not injected**: parsed as `image/svg+xml` into an inert document, refused on an XML parse error or a non-SVG root, walked and refused on any element outside a 15-element allowlist or any `on*` / `href` / `*:href` / `base` attribute, then `document.importNode`d. Refuse, not scrub. Still no `dangerouslySetInnerHTML` or `innerHTML` anywhere in `src/`. Verified in Chrome: the 15-element set is *observed* output (identical for the golden fixture and for a diagram of nothing but hostile labels), hostile labels do not execute (`<script>` labels vanish, `onerror=`/`onload=` survive only as literal text), the fixture draws once under StrictMode, and **all four refusal paths refused**. *(Corrected 2026-09-01 by the test suite below: this read "all four refusal paths fired at their intended layer". Three did. `click … href` emits a plain `href` on an `<a>`, not the `xlink:href` ADR-025 described, so the element allowlist and the attribute denylist refuse it rather than the XML parse. Still refused; see ADR-025's Correction.)* `fixture.ts`'s `componentDiagram` was rewritten in the same change: it had been using repository-derived strings as Mermaid **node identifiers**, contradicting ADR-024's central guarantee. **One thing this does not do**: it is **not wired into `App.tsx`** — nothing imports it, so the running app is unchanged. *(The second thing it used to not do — "there are still no frontend tests, so every browser observation above is manual and unrepeatable" — was closed later the same day; see the next item.)* The supply-chain cost is worth knowing: 159 transitive packages, including a second copy of `cytoscape`; `npm audit` clean
- [x] **A first frontend test, over `ComponentDiagram.tsx` before anything else.** **Done 2026-09-01** — `frontend/vitest.config.ts`, `frontend/src/ui/ComponentDiagram.test.tsx`, `frontend/src/no-markup-sinks.test.ts`: **vitest 4.1.11 + jsdom 30.0.1** (both checked against the registry, not recalled), **35 tests**, `npm test`. It covers the 15-element allowlist against mermaid's real output for the hostile fixture, the panel's live DOM after mounting, all four refusal paths plus the not-an-SVG path — each isolated by stubbing `mermaid.render`'s return value so the exact message can be pinned — that a mermaid parse failure surfaces only the fixed message and never the exception (mermaid quotes repository text in its parse errors, and that is asserted), the `htmlLabels: false` and non-`loose` `securityLevel` controls observed through their effects, and that nothing in `src/` uses `dangerouslySetInnerHTML` / `innerHTML` / `outerHTML` / `insertAdjacentHTML` / `document.write`. It earned its keep immediately: it **found a factual error in ADR-025** — `click … href` emits a plain `href`, not `xlink:href`, so the XML gate is not what refuses it — and it turned up a parse5 conformance gap that would make a correct component look broken under jsdom. Both are recorded in ADR-025's Correction
- [ ] **Drive `feDropShadow` once in a real browser and record the result.** The suite above patches `fedropshadow` → `feDropShadow` into parse5's SVG tag-name adjustment table, because parse5 8.0.1 carries only 36 of the spec's 37 rows and mermaid emits `<feDropShadow>` in every dark-theme flowchart. Every element-allowlist assertion in the suite therefore rests on the assumption that Chrome implements the full table and restores the camelCase name. That is what the spec requires and the original interactive session implied it by rendering at all, but it has not been checked deliberately. Cheap, and it is the assumption the rest of the suite hangs from
- [ ] **Tests for the rest of `frontend/`.** The suite above covers one panel. `App.tsx`, `GraphCanvas.tsx`, `Inspector.tsx`, `ServiceMap.tsx`, `graphStore.ts` and `client.ts` have no tests of their own; `no-markup-sinks.test.ts` is the only thing that touches them, and only as a grep. Sharing `backend/tests/fixtures/analyze_response_golden.json` with a frontend schema test is the highest-value next one
- [x] Service map panel: grouped list, route → file → summary where one exists. **Done 2026-09-01** — `frontend/src/ui/ServiceMap.tsx`. This is the first sink `ServiceEndpoint.summary` has of its own; the diagram embeds route summaries inside labels, which is not the same as rendering the field. It is **not wired into `App.tsx`** and has **no test** — unlike `ComponentDiagram.tsx`, which gained one the same day, leaving this the one panel with neither — so the guardrail below is ticked for it on a code-path reading only — the summary is a React child and there is no markup construction anywhere near it, but nobody has inspected this panel's DOM
- [ ] **Put both unwired panels in the layout.** `ComponentDiagram.tsx` and `ServiceMap.tsx` exist and render, but nothing imports either, so both are absent from the running app. This is one layout question — where two panels go beside the canvas and the inspector — and was deliberately left out of both render changes
- [x] **Rendering guardrail:** `description`, `summary`, and the diagram source are repository content — render as React text nodes / hand the source to the Mermaid renderer. Never build an HTML string, never `dangerouslySetInnerHTML`. This is the one ADR-012 security rule that survives ADR-013, because the sink outlived the model. **Honoured for both sinks that exist** (2026-09-01): `Inspector.tsx` interpolates `description` as a React child, `ComponentDiagram.tsx` adopts the rendered diagram out of an inert document behind an element allowlist (ADR-025), and `grep` finds no `dangerouslySetInnerHTML` or `innerHTML` anywhere in `frontend/src/`. A fixture description carries a `<script>` string as a standing probe, and `fixture.ts`'s diagram now carries a second one in a label. **Upgraded from "a claim about the code path" — the DOM has now been inspected**, in Chrome on 2026-09-01, for the diagram sink. *(The old caveat overstated the gap even when it was written: the `description` sink had already been checked in the DOM the same day — CURRENT_STATE.md's verification block records the fixture's `<script>` arriving as `&lt;script&gt;` with zero `<script>` elements under the `<aside>` — so the diagram is the **second** sink to be observed, not the first. SECURITY.md's row carries that retraction.)* For the diagram: the 15-element allowlist is mermaid's *observed* output rather than a guess, the emitted SVG contained no `<script`, no `on*=` and no `foreignObject`, deliberately hostile labels (`<script>…</script>`, `<img src=x onerror=…>`, `<svg onload=…>`, `</text><script>…</script>`) executed nothing and survived only as literal text or not at all, and all four refusal paths were driven and each refused. **Upgraded again later the same day: the observations are now automated** — `ComponentDiagram.test.tsx` and `no-markup-sinks.test.ts`, 35 tests under vitest + jsdom, re-run on every `npm test`, so a regression in the allowlist, the config or the injection path fails a test. *(Corrected 2026-09-01: this used to say "each fired at its intended layer" and that the observations are "manual and interactive, and `frontend/` still has no tests … the evidence exists and is not repeatable". The suite closed the second and corrected the first — `click … href` is refused by the element and attribute checks, not the XML parse. ADR-025 carries it.)* What the upgrade still does **not** cover: the suite is **jsdom, not a browser**, running on two SVG-metric stubs and one patched parse5 row; and the panel was driven from `fixture.ts`, never from a server response. Cytoscape node labels are a third sink and are safe for a different reason — the `label` style property paints glyphs to a `<canvas>` and parses no markup — and only *names* are labelled. `summary` **now has a sink**, `ServiceMap.tsx`, landed the same day: it interpolates the summary as a React child, which is the `Inspector.tsx` pattern and carries the same structural argument, but that panel's DOM has **not** been inspected and it is unwired, so it is ticked on a code-path reading only. The box is ticked for `description`, the diagram, and — more weakly — `summary`

## Day 3 — Deploy + harden

- [ ] Backend → Railway/Render/Fly (ADR-011) — verify tree-sitter's native grammar wheels load in the target runtime before committing to it
- [ ] Frontend → Vercel, `VITE_API_URL` pointed at the backend, CORS locked to the Vercel domain
- [x] Rate limiter + concurrency gate on `/api/analyze` (ADR-008 design). **Done 2026-09-01** — `app/api/rate_limit.py` (`SlidingWindowLimiter`, `ConcurrencyGate`, ~65 lines), held on `app.state` (one pair per app instance, so a test's `create_app()` starts with an empty clock). `app/api/routes.analyze` checks the per-IP window (`RATE_LIMIT_ANALYZE`, `RATE_LIMIT_ANALYZE_HOURLY`) before `parse_github_url` runs, raising `RateLimitedError` (429, `Retry-After` header) from `math.ceil` of the wait; the concurrency gate (`MAX_CONCURRENT_ANALYSES`) is checked immediately before `asyncio.to_thread` would spawn the worker, raising `ServerBusyError` (503) instead — so a rejected request never occupies a thread. `AppError` gained a `headers()` hook (default `{}`) so `Retry-After` reaches the client without joining the three-key body contract. Tests in `tests/test_rate_limit.py` (unit-level window/gate arithmetic plus three route-level tests: 429 with `Retry-After`, per-IP isolation, and a 503 proven to fire before the worker thread starts). CORS is still open
- [x] ~~Body-size middleware (4 KiB cap, `content-length` **and** chunked-body byte counting)~~ — **done early, on Day 1**, because the routes could not be tested against the threat model without it. `app/api/middleware.BodySizeLimitMiddleware`, both halves, refusing before the application reads a byte
- [ ] Extend `backend/scripts/smoke.py` past ingestion once the graph exists: medium TS repo, one with circular imports, and one whose files carry JSDoc headers (to exercise descriptions on real comments rather than fixtures). The ingestion half already runs green against three real repositories
- [ ] **Adversarial run against a *hostile* repository.** The happy path is verified (`backend/scripts/smoke.py`, three real repositories) but **no security control has ever been triggered by data we did not construct** — every guard is fixture-only. This is the gap that matters most before a public deploy
- [ ] Security pass: malicious URL rejected, localhost/private-IP rejected, a `.env`-containing repo produces no leaked content in the graph or response, and a repository comment containing `<script>` / control characters / a megabyte of text yields a bounded plain-text description. *(Mostly closed on Day 1 now that a response exists: `tests/test_api_routes.py` asserts a malicious URL is refused without being echoed and that a `.env` reaches no part of a real response body. What is left is the megabyte-comment assertion **over a response** rather than over the extractor, and the rendering side.)*
- [ ] README: architecture summary, security model summary, how to run locally, link to the live Vercel URL

## Deferred (post-MVP, not cancelled)

- `tsconfig.json` `paths` / `baseUrl` / workspace-package resolution in `resolver.py`. **The seam is decided, not open**: ADR-017 fixes it as parse-in-the-pipeline-loop, carried already-narrowed on `RepositoryAnalysis` — not as raw bytes (breaks ADR-016's tested invariant) and not as a second archive pass (ADR-003 keeps nothing, so it means a second download). Needs a JSONC reader in the pipeline, and slots into `resolver._resolve_one` between the relative attempt and the external fallback
- ~~Force-directed layout refinement pass (ADR-004 phase two)~~ — moot: ADR-022 (2026-09-01) superseded ADR-004 outright in favor of a Cytoscape.js layout, so there is no worker phase two to schedule
- `POST /api/source` raw code viewer UI — **and now the whole ADR-007 mechanism with it**: the endpoint, HMAC token issuance and verification, and the serving-time re-application of `is_secret_path`, deferred as one unit. ADR-013 deleted `/api/explain`, which was the only MVP caller keeping that machinery in scope. `GraphNode.sourceToken` stays in the wire contract and stays `None`
- **Decorator-style route detection (NestJS `@Controller` + `@Get`), Fastify's `fastify.route({method, url})` object form, chained `router.route('/x').get(h)`, and the Next.js Pages Router.** Each is a real route shape `app/analysis/routes.py` misses today, and each has a test pinning the miss so it stays deliberate (ADR-021). The decorator case needs class-level prefix joining to be useful at all, and a wrong join reports a URL that does not exist — which is the one failure mode the module is built to avoid
- Any LLM/AI feature (ADR-012's model-written C4 diagrams, file explanations, route summaries). **Not deferred — removed.** Reinstating requires superseding ADR-013, not merely scheduling it
- Richer description sources: directory `README.md`, `package.json` `description` for monorepo packages. Considered and deliberately excluded from MVP — the file header comment alone is the scope (ADR-013)
- Search / tree panel, camera-focus-on-search
- `docker-compose.yml` + both Dockerfiles, verified `docker compose up` from a clean clone (ADR-001's original target; ADR-011 is the MVP-only substitute)
- End-to-end pass over the full PRD §15 test matrix (monorepo structure, huge repo, thousands of files, deep nesting, oversized individual file)
- Confirm bounded RSS under load; confirm no repository content in logs
- Decide whether `extract_imports` should report skips, so parser-level drops can be counted in stats. Today an oversized, binary, or pathologically malformed file is reported by yielding nothing, which is indistinguishable from a file with no imports — so it stays in the analysis as a node with real bytes, real lines, and zero imports, and is absent from `skipped`. A test pins the current behaviour so it is deliberate rather than incidental
- CSP `style-src 'unsafe-inline'` removal via `sheet.insertRule()`. Note that a CSP now has a *second* job: `ComponentDiagram.tsx` keeps `<style>` on its element allowlist because mermaid scopes its generated CSS with `#id` selectors and the panel is unreadable without it, so the recorded residual is `url()` exfiltration from a crafted stylesheet — unreachable from a response our own backend generates (repository text cannot reach a `classDef`, ADR-024) and closed by `default-src 'self'` (ADR-025)
- Picking performance profiling at 5000+ nodes; `three-mesh-bvh` only if measured necessary
- `frameloop="demand"` battery optimization
- CI (run backend and frontend tests on push)

## Blocked

- Nothing.
