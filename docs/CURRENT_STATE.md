# Current State

## Current Goal

> **2026-08-31 — scope narrowed to a 3-day MVP sprint.** See [DECISIONS.md](DECISIONS.md) ADR-011 and ADR-012, and the day-by-day breakdown in [TODO.md](TODO.md). This does not replace the PRD's goal below — it is a deliberately reduced first slice of it, with three additions (C4 diagram, service map, per-file explanations) the original PRD did not specify, and two deployment/scope amendments (split Vercel+PaaS deploy instead of `docker compose up`; sphere-packing-only layout instead of the two-phase force layout) recorded as amendments to ADR-011/ADR-001 and ADR-004 respectively, not replacements.

> **2026-08-31, later the same day — the LLM layer is removed entirely (ADR-013 supersedes ADR-012).** It did not fit the deadline. **The project now contains no LLM at all**: no `app/llm/`, no `POST /api/explain`, no provider, no API key. The three features survive as deterministic output — a file's **description is its own leading header comment** (JSDoc / block / `//` run), a route's **summary is the comment above the handler**, and the diagram is **generated from the graph itself** (top-level directories as containers, external packages as external systems, detected routes as the API surface), with the response field renamed `c4` → `componentDiagram` because it is not a C4 model. Route detection was always deterministic and is unchanged.
>
> Two consequences worth knowing before you plan anything: **`/api/analyze` is now the MVP's only network-touching endpoint** (the HMAC token mechanism went back to Deferred with `/api/source`, since `/api/explain` was its only in-scope caller), and **the whole response is a pure function of the commit**, which makes a golden-file test over an entire `AnalyzeResponse` possible for the first time.

> **2026-09-01 — visualization is 2D via Cytoscape.js, not 3D via Three.js/React Three Fiber (ADR-022, supersedes ADR-002 and ADR-004).** Owner-driven scope change to minimize bespoke frontend code. Nothing under `backend/` is affected — this changes only the still-unbuilt frontend's rendering/layout approach, and [PRD.md](../PRD.md) is updated to match.

> **2026-09-01 — the backend is end to end (ADR-023).** `app/api/` exists, so a repository URL over HTTP now produces an `AnalyzeResponse`. Three things this closes, all of which had been open since the contract layer: **`app/errors.py` has something that returns it** (four handlers, covering every way out of the application); **`MAX_NODES`/`MAX_EDGES` are enforced**, by `build_graph` building a smaller graph on request rather than the router slicing a large one, so nothing in `stats` is falsified; and **`MAX_REQUEST_BODY_BYTES` is enforced**, checking `Content-Length` *and* counting delivered bytes, before the application reads a byte. The analysis runs on a worker thread and there is deliberately **no `asyncio.wait_for`** around it. What remains before a deploy: the component-diagram generator, the rate limiter and concurrency gate, CORS, and the whole frontend.

> **2026-09-01, later the same day — the rate limiter and concurrency gate landed (ADR-008).** `app/api/rate_limit.py` adds `SlidingWindowLimiter` (per-IP, checked before `parse_github_url` even runs) and `ConcurrencyGate` (global, checked immediately before the worker thread would be spawned) — both hand-rolled, in-process, no Redis, per the ADR. `/api/analyze` now returns **429 with a `Retry-After` header** over `RATE_LIMIT_ANALYZE`/`RATE_LIMIT_ANALYZE_HOURLY`, and **503** over `MAX_CONCURRENT_ANALYSES`, before a rejected request ever occupies a thread. `AppError` gained a `headers()` hook (default `{}`) so `Retry-After` reaches a response without joining the three-key body contract — `RateLimitedError` is now the one exception carrying per-instance data, and it is a wait time, not text that could echo user input. Both structures live on `app.state`, one pair per app instance, so nothing leaks between a production process and a test's `create_app()`. This closes the sharpest resource gap named throughout this document. Component diagram, CORS, and the frontend are what remain before a deploy.

Build the MVP defined in [PRD.md](../PRD.md): paste a public GitHub URL → safely analyze a TS/JS repository → render its dependency graph in a navigable 2D view, plus a deterministic component diagram, an API service map, and per-file descriptions quoted from the repository's own comments (ADR-013).

Steps 1-5 of the build order are done and green: **the backend contract** (config, errors, models, logging), **the URL/egress security boundary** (`security/url_validation.py`, `security/net_guard.py`), **the GitHub client** (`fetch/github.py`) — the first module in the project that can open a socket — **the streaming archive reader** (`fetch/archive.py`), and **the import extractor** (`analysis/parser.py`). Two further security modules, **`security/secret_filter.py`** and **`security/path_safety.py`**, were implemented ahead of their call sites.

**The modules are now joined.** `analysis/pipeline.analyze_repository` is the first code path in the project that calls one of them from another: it sends the download `github.py` builds, streams `response.iter_raw()` into `archive.iter_source_files`, applies `is_secret_path` to every yielded path before the bytes reach a parser, picks the grammar by extension, and runs `extract_imports`. It harvests the commit SHA from the archive root, enforces `MAX_SOURCE_FILES`, counts the skips, and raises `NoSupportedFilesError` when nothing parseable survives.

> **Corrected 2026-08-31 against the code.** An earlier revision of this section described `iter_bytes()`, an `ArchiveRoot` out-parameter, a `grammar_for` function, and a `pipeline.analyze -> Ingested` contract carrying `(path, content, imports)`. **None of those are in the tree.** Two implementations of this task were developed in parallel (PR #2 and PR #3) and the merge kept PR #2's code alongside PR #3's documentation. The code has `iter_raw()`, `ArchiveInfo`, a `_BY_EXTENSION` dict, and `analyze_repository -> RepositoryAnalysis`, whose `SourceFile` is deliberately **content-free** (ADR-016). The `iter_bytes()` difference is not cosmetic: httpx transparently gunzips a `Content-Encoding` response, which would silently change what `MAX_DOWNLOAD_BYTES` and the compression-ratio denominator measure.

**Resolution now exists, at MVP scope.** `analysis/resolver.resolve_imports` takes the `RepositoryAnalysis` the pipeline produces and answers every specifier: a file in the analysis, an external package, or unresolved. It is a pure function with no I/O, no clock, and no logger — resolution is set membership against `RepositoryAnalysis.files` (ADR-016), which is what makes a dangling edge unrepresentable rather than something the graph builder filters out. Scope is deliberately relative-imports-plus-bare-as-external; `tsconfig` `paths`, `baseUrl`, and workspaces are Deferred, with the seam that will carry them decided in ADR-017 and stubbed.

**Descriptions now exist.** `analysis/descriptions.header_description` quotes each file's leading header comment — JSDoc, plain block, or a run of `//` — normalizes it (markers off, whitespace collapsed, non-printables dropped, capped at `MAX_DESCRIPTION_CHARS`) and returns `None` when nothing survives, which is the ordinary case. The pipeline calls it in the loop where the bytes are still in hand and carries the result on `SourceFile`; the graph builder copies it onto file nodes. **It takes no tree** (ADR-020): a header comment is at byte 0, where none of JS's lexical ambiguity applies, so a 4 KiB prefix scan gives the same answer a parser would and costs ~0.7 s across 3000 files in the worst adversarial case instead of a second ~5.7 s parse. This is **the first repository-authored *text* to reach a response body** — everything before it was paths, counts, and line numbers — so it is also the first time ADR-013's surviving security rule has a call site.

**The graph now exists.** `analysis/graph_builder.build_graph(analysis, resolved)` is the resolver's first caller, and as of 2026-09-01 it has a caller of its own. It emits one node per file, one per inferred ancestor directory, and one for the repository root (`"."`, `parent: None`, named for the repo); one edge per `RESOLVED` import, deduplicated, with self-edges dropped; and the `Stats` the PRD specifies. Node `id` *is* the repository-relative path, ordering is by path *components* rather than by path string, and the determinism the pipeline and resolver deliberately deferred is done here and mutation-tested. Shape decisions are ADR-018.

**The diagram now exists, and it is the first output of this project that is a *format* rather than data.** `analysis/component_diagram.build_component_diagram(nodes, edges, stats, service_map)` is the graph builder's first consumer and the last analysis module before routing. Top-level directories become containers, external packages become **one** box carrying a total, and detected routes become the API surface, all as Mermaid `flowchart LR` source (ADR-024). The external box is unnamed on purpose: ADR-005 kept package *names* out of the graph, `resolver.ResolvedImport.specifier` is the last place one exists, and this module is deliberately not given it — naming them would put a new class of repository-authored text into a response body, which is a decision and not a detail. The safety argument is that **node identifiers are synthetic** (`c0`, `r0`, `ext`), so a directory name, a route path and a comment have exactly one position in the output — inside a quoted label — and none in the syntax; the sanitizer is the second line, not the only one. `MAX_COMPONENT_DIAGRAM_CHARS` finally has a producer and is applied *while writing*, because a truncated Mermaid document is an unclosed `subgraph` rather than a smaller diagram.

**Routes now exist, and getting them required a seam.** `analysis/routes.detect_routes` yields `ServiceEndpoint` records — method, path, file, 0-indexed line, optional summary — from two queries: Express-style member calls (`app.get('/users/:id', h)`, any receiver) and the Next.js App Router file convention (`app/**/route.ts` exporting `GET`). It is the first module to read a tree it did not build, because **`parser.py` split in two** (ADR-021): `parse_source` owns every guard between untrusted bytes and tree-sitter and returns a `Tree`, and `extract_imports` is now just the query over one. The alternative — a second module that re-parses, mirroring `descriptions.py` — was rejected on *guard duplication*, not cost: a second parse means a second copy of `_is_pathological`, which is the only thing standing between the query engine and an eleven-minute traversal. ADR-020's reason for keeping descriptions *out* of the tree does not transfer, and the asymmetry is the design: a header comment exists whether or not the parse succeeded, a route does not.

The verb set is deliberately not the whole filter. `map.get('key')` is a member call whose property is an HTTP verb, so two further conditions close it — the first argument must be a string literal starting with `/`, and there must be a handler argument after it. This is the phantom-dependency problem with a worse blast radius: a spurious edge is one line in a graph of thousands, a spurious endpoint is one row in a service map of six.

**The routes now exist, and they are the last seam.** `app/api/routes.analyze` validates the URL, sends the whole blocking span — download, parse, `resolve_imports`, `build_graph`, model construction — to one worker thread, and returns an `AnalyzeResponse`. `app/api/app.py` holds the factory and **four** exception handlers (`AppError`, `RequestValidationError`, Starlette's `HTTPException`, and a catch-all), which between them are every way a response can leave the application; one `_body()` constructs all of them. `app/api/middleware.py` assigns a per-request id and caps the request body. `app/main.py` is the ASGI entry point and the only place that configures logging.

Two decisions inside that are worth carrying (full reasoning in ADR-023). **`MAX_NODES`/`MAX_EDGES` are applied by asking `build_graph` for a smaller graph**, taking the second of the two branches ADR-018 offered, because slicing its output falsifies eight computed numbers and can dangle an edge. Nodes are cut to a *prefix of component order*, which is closed under `parent`, so nothing is orphaned. And **there is deliberately no `asyncio.wait_for`** around the analysis: it cannot kill a thread, so it would return a 504 while the work continued and turn a client retry into a thread leak.

**The frontend now exists, and it has spoken to the backend.** `frontend/` was scaffolded on 2026-09-01 (Vite 8, React 19, TypeScript 5.9.3, Tailwind v4, Zustand 5, zod 4, Cytoscape.js 3.34 — all pinned exactly, `package-lock.json` committed). It holds the zod mirror of the wire contract, a Cytoscape.js canvas, a Zustand store, and an inspector. *(Corrected 2026-09-01, later the same day: this used to say the canvas "renders directory hierarchy as compound nodes" as the visible structure. ADR-026 demoted that: `parent` still flows into `data.parent` for layout grouping, but directories no longer render as boxes — see the Rendering entries below and ADR-026.)* **The API client landed the same day the routes did.** `src/api/client.ts` posts to `/api/analyze`, validates the body with `AnalyzeResponseSchema` on success and `ApiErrorSchema` on failure, and `App.tsx` now has a landing page with a URL input in front of the canvas instead of seeding the store from `src/graph/fixture.ts`. Run against three real repositories (`sindresorhus/p-limit` among them) through the actual dev server — proxied at `/api` to the backend, since the backend has no CORS headers yet (Day 3) — the schema validated the real response on the first try; nothing in `schema.ts` needed loosening. The fixture stays in the tree, unreferenced by the app, as a schema regression probe.

**Three sinks for repository text now exist; one of them is exercised by the running app.** `Inspector.tsx` interpolates `description` as a React child, so the "text node, never `dangerouslySetInnerHTML`" half of ADR-013's rule has a call site — and since the client landed it is exercised with repository-authored text from a real parse rather than a hand-written string. The other two arrived the same day and **neither is wired into the layout**: `src/ui/ComponentDiagram.tsx` draws `componentDiagram` through `mermaid`, which is the one place in the app where repository text comes back as markup rather than sitting beside it (ADR-025), and `src/ui/ServiceMap.tsx` renders `ServiceEndpoint.summary`, which had no sink of its own before. The diagram panel is browser-verified — but from `fixture.ts`, never from a server response; the service map has not been rendered at all. *(**Corrected 2026-09-01**: this parenthetical used to say the guardrail was "a claim about the code path … not about an observed browser render; nobody has loaded the page and inspected the DOM". That was already stale against this file's own verification list below, and staler still now that two sinks have been inspected in a live DOM. `grep` finds no `dangerouslySetInnerHTML` or `innerHTML` in `src/`. What replaces the caveat is narrower and less comfortable: the observations are **manual**, there are no frontend tests, and nothing re-runs them.)* *(**Corrected again 2026-09-01**, later the same day: the diagram panel's observations are now re-run by `src/ui/ComponentDiagram.test.tsx` on every `npm test`. The remaining caveat is that the suite is jsdom rather than a browser, and that `ServiceMap.tsx` still has neither a test nor an inspected DOM.)*

**What that does *not* mean.** `safe_relative_path` still has **no caller**; do not read its presence as protection, and note that neither the resolver nor the builder needs it (there is no disk, so there is no base directory to stay inside of). Concurrent analyses are bounded now (ADR-008, below) — that gap is closed, not open.

**The real-network gap is closed, on the happy path only.** `backend/scripts/smoke.py` analyzed `sindresorhus/p-limit`, `pmndrs/zustand`, and `sindresorhus/ky` end to end on 2026-08-31 and confirmed the `RepositoryNotFoundError` and `NoSupportedFilesError` branches against real responses. The pytest suite remains hermetic and fixture-built — the smoke script is a separate entry point, never collected (ADR-014).

## Working

**The whole backend request path, end to end, and its tests.** `app/api/` now holds `routes.py`, `middleware.py`, `rate_limit.py`, and `app.py`, with `app/main.py` as the entry point; `app/analysis/` holds `deadline.py`, `parser.py`, `pipeline.py`, `resolver.py`, `descriptions.py`, `graph_builder.py`, `routes.py`, and `component_diagram.py` — the whole analysis half of the backend; and `app/security/` holds all four modules the MVP needs — the fifth (HMAC tokens) went out of scope with `/api/explain` (ADR-013). The rate limiter and concurrency gate landed 2026-09-01 (ADR-008). Missing from the routing layer, still Day 3: CORS.

**Plus a frontend** that builds, typechecks under `strict` (with `exactOptionalPropertyTypes` and `noUncheckedIndexedAccess`), and renders against a real `/api/analyze` response behind a landing page and a loading state. It has **35 tests of its own** as of 2026-09-01 (vitest 4.1.11 + jsdom 30.0.1, `npm test`), covering `ComponentDiagram.tsx` and a repo-wide markup-sink grep — everything else in `src/` is untested, and the backend's 1458 are still the bulk of the suite. *(Corrected 2026-09-01: this read "It has **no tests of its own**; the backend's 1458 are still the whole suite.")*

| Path | Notes |
|---|---|
| `LICENSE` | MIT |
| `README.md` | Two-line description |
| `PRD.md` | Product spec — now staged in git |
| `CLAUDE.md`, `docs/*` | This documentation, created 2026-08-29 |
| `.gitignore` | Both stacks; `.env.example` is un-ignored |
| `backend/pyproject.toml` | Pinned deps + pytest/ruff/mypy config; hatchling build backend |
| `backend/uv.lock` | 39 resolved packages, committed |
| `backend/app/config.py` | `Settings` — all 32 non-secret limits + 2 `SecretStr` secrets, `extra="ignore"`. (The "22 limits" this row claimed was stale well before ADR-013; counted from `model_fields`, not recalled.) |
| `backend/app/errors.py` | `ErrorCode` + 14 `AppError` subclasses; fixed 3-key body |
| `backend/app/models/` | `graph.py`, `api.py`, re-exporting `__init__.py`. `AnalyzeResponse` carries `serviceMap`/`componentDiagram` and `GraphNode` carries `description`; all three default to absent. **All three now have producers** — `description` from ADR-020, `serviceMap` from ADR-021, `componentDiagram` from ADR-024 |
| `backend/app/logging_setup.py` | JSON-line formatter + `RedactingFilter` |
| `backend/app/security/url_validation.py` | `parse_github_url` → frozen `RepoRef`; strict grammar, ASCII-only, allowlisted host |
| `backend/app/security/net_guard.py` | `validate_download_url` (equality allowlist) + `assert_public_ip` (resolved-IP check). Both are now called by `fetch/github.py` on every redirect |
| `backend/app/fetch/github.py` | `create_client` (`follow_redirects=False`, `trust_env=False`, no `Authorization` default), `get_repo_metadata` (preflight, size gate, 403/404 collapse), `get_download_url` (validated single hop), `download_request` (the credential-free download GET) |
| `backend/app/fetch/archive.py` | `iter_source_files` — streams the download through `_CountingRawStream` → `gzip` → `_DecompressedStream` → `tarfile("r|")`, yielding `(PurePosixPath, bytes)` per acceptable regular file. `Limits` is a no-default view onto `Settings`. **`ArchiveInfo`** is the optional out-parameter carrying the commit SHA and the per-reason skip counts (ADR-015) |
| `backend/app/security/secret_filter.py` | `is_secret_path` — exact names, prefixes, extension suffixes, and excluded directories, matched case-insensitively over path components. Pure, no I/O. Called by `analysis/pipeline.py` on every yielded path |
| `backend/app/security/path_safety.py` | `safe_relative_path` — realpath the base *and* the candidate, then require `commonpath` to equal the base. Raises `ValueError`, never an `AppError` (it is a utility, not an HTTP boundary). **No caller**, by design |
| `backend/app/analysis/deadline.py` | `Deadline` — frozen, monotonic; `check()` raises `AnalysisTimeoutError` |
| `backend/app/analysis/parser.py` | **Two entry points as of ADR-021.** `parse_source` — size cap, binary sniff, BOM strip, deadline on both sides, parse, pathological-tree guard — returns a `Tree` or `None`; it is the seam every reader of a tree goes through, so a guard exists once. `extract_imports(tree, path)` is the import query over `QueryCursor.matches()`, yielding `(specifier, 0-indexed line)` and resolving nothing. `string_literal_text` (was `_specifier`) is shared with `routes.py`. Never raises except `AnalysisTimeoutError`. Called by `analysis/pipeline.py` |
| `backend/app/analysis/pipeline.py` | `analyze_repository(RepoRef) -> RepositoryAnalysis` — **the seam**. One `Deadline` per request; preflight → validated redirect → credential-free streamed download (`iter_raw`) → archive reader → `is_secret_path` → grammar by extension (`_BY_EXTENSION`) → `extract_imports`. Returns **content-free** `SourceFile` records (path, language, `size_bytes`, `loc`, imports) plus the commit SHA, the skip tally, `truncated`, and `imports_truncated` (ADR-016). Enforces two caps — `MAX_SOURCE_FILES` on the file count and `MAX_IMPORTS` on the running import total, the latter bounding the clockless resolution phase (ADR-019). Refuses a `private` repository. Resolves nothing. Opens no file (ADR-003) |
| `backend/app/analysis/resolver.py` | `resolve_imports(RepositoryAnalysis) -> tuple[ResolvedImport, ...]` — one record per import: `RESOLVED` to a file in the analysis, `EXTERNAL`, or `UNRESOLVED`. Relative specifiers only; precedence is TS ESM rewrite → literal → extension append → directory `index.*`. Pure — no I/O, no filesystem, no clock, no `Deadline`, and **no logger** (SECURITY.md forbids logging specifiers). Paths compared byte-exact. Called by `analysis/graph_builder.py` (ADR-017) |
| `backend/app/analysis/descriptions.py` | `header_description(bytes) -> str \| None` — the file's leading header comment, quoted. A 4 KiB prefix scan, **no tree, no grammar, no `Deadline`, and no logger** (ADR-020). Splits into a locator (`header_description`, byte-0 only) and a normalizer (`normalize_comment`, which takes a comment's own text with markers and is the half `ServiceEndpoint.summary` will share). Total — every input it cannot use returns `None`, including binary, undecodable, and empty. Called by `analysis/pipeline.py` |
| `backend/app/analysis/routes.py` | `detect_routes(tree, path, deadline, settings) -> Iterator[ServiceEndpoint]` — two queries over the tree `parse_source` built: Express-style member calls and the Next.js `app/**/route.ts` convention. Verb set plus a leading-`/` path plus a handler argument, so `map.get('key')` is not an endpoint. `summary` is the comment directly above the enclosing statement, via `descriptions.normalize_comment` with the endpoint cap. **Lazy**, so the pipeline's `MAX_SERVICE_ENDPOINTS` bounds work and not just output. Called by `analysis/pipeline.py` |
| `backend/app/analysis/graph_builder.py` | `build_graph(RepositoryAnalysis, tuple[ResolvedImport, ...], *, limits=None) -> (nodes, edges, stats)` — one node per file plus inferred ancestor directories plus the repository root; one edge per `RESOLVED` import, sorted by path components, deduplicated, self-edges dropped. Node `id` is the path. Per-node `imports`/`importedBy` come off the finished edge set, so `sum(imports) == sum(importedBy) == len(edges) == stats.dependencies`. Pure — no I/O, no clock, no `Deadline`, and **no logger**, both pinned by test. **`limits` caps it at `MAX_NODES`/`MAX_EDGES`** (ADR-023, 2026-09-01, superseding ADR-018's "uncapped by design"): the node list is cut to a prefix of component order — closed under `parent`, so nothing is orphaned — edges naming a dropped node go with it, the edge cap applies next, and every counter is derived afterwards. `None` builds the whole graph, which is what every caller but the router wants. Called by `app/api/routes.py` |
| `backend/app/analysis/component_diagram.py` | `build_component_diagram(nodes, edges, stats, service_map) -> str \| None` — Mermaid `flowchart LR` from the finished graph (ADR-013, ADR-024). Top-level directories as containers (derived from *file* paths, so ADR-018's file/directory collision cannot delete one), external packages as **one** box with a total, detected routes as the API surface. Node ids are synthetic, so repository text sits only inside a quoted label and never in the syntax. `MAX_COMPONENT_DIAGRAM_CHARS` enforced while writing. Pure — no I/O, no clock, no `Deadline`, and **no logger**, both pinned by test |
| `backend/app/api/routes.py` | `POST /api/analyze` and `GET /api/health`. Checks the per-IP rate limiter, validates the URL, checks the concurrency gate, then sends the whole blocking span — download, parse, resolve, build, model construction — to one `asyncio.to_thread`. Applies `GraphLimits.from_settings`. Logs counts only, never a URL, an owner, a name, or a path. Deliberately **no `asyncio.wait_for`** (ADR-023). Calls `build_component_diagram` on the **capped** nodes/edges, so the diagram describes the graph in the response |
| `backend/app/api/rate_limit.py` | `SlidingWindowLimiter` (per-IP, multi-window, in-memory deque) and `ConcurrencyGate` (global, non-blocking counter) — ADR-008. Neither reads `Settings` itself; the caller passes windows/limit per call, so a test can tighten them without rebuilding the instance. One pair held on `app.state` per `FastAPI` instance |
| `backend/app/api/middleware.py` | `RequestIdMiddleware` — one id per request, on `scope["state"]`, echoed as `X-Request-ID`, never read from an inbound header. `BodySizeLimitMiddleware` — `MAX_REQUEST_BODY_BYTES` by declared `Content-Length` **and** by counting delivered bytes, refusing **before the application runs**. Pure ASGI, not `BaseHTTPMiddleware`, which would buffer the body first. Writes its own response, because FastAPI turns any exception from a body read into a 400 |
| `backend/app/api/app.py` | `create_app()` plus the four exception handlers that make `app/errors.py`'s contract real — `AppError`, `RequestValidationError` (→ bare `INVALID_REQUEST`; pydantic's `detail` echoes the offending input), Starlette's `HTTPException` (a 404/405 would otherwise be the only non-contract bodies in the service), and a catch-all 500. One `_body()` builds every one. `/docs` and `/redoc` disabled (CDN-loaded scripts); `/openapi.json` kept |
| `backend/app/main.py` | `uvicorn app.main:app`. Calls `configure_logging()` and then `create_app()` — the factory has no side effects so a test can build an app without tearing `caplog`'s handlers off the root logger |
| `backend/tests/fixtures/tarballs.py` | `make_tar`, `make_source_tar`, `make_member_with_name`, `make_pax_name`, `make_symlink_member`, `make_hardlink_member`, `make_oversized_header`, `make_many_members`, `make_bomb`, `chunked`, `noise` |
| `backend/tests/conftest.py` | Session-scoped autouse fixture blocking `getaddrinfo`, `gethostbyname`, `create_connection`, and `socket.connect`/`connect_ex`. Raises `NetworkAccessAttempted`, a `RuntimeError` — deliberately *not* an `OSError`, so it travels straight through `assert_public_ip`'s handler instead of being swallowed as a rejection |
| `backend/tests/` | 1458 tests across config, errors, models, logging, URL validation, net guard, GitHub client, archive reader, deadline, secret filter, path safety, parser, the pipeline, the resolver, the description extractor, the graph builder, route detection, the component diagram, the HTTP boundary, and the rate limiter / concurrency gate |
| `backend/tests/test_api_routes.py` | 25 cases over the real application, driven through `httpx.ASGITransport` — no socket, and **not** `starlette.testclient`, whose httpx integration is deprecated in this Starlette and would trip `filterwarnings = ["error"]`. Note the name: `test_routes.py` is route *detection* |
| `backend/tests/fixtures/component_diagram_golden.mmd` | The whole Mermaid output for one small repository, compared byte for byte. The first golden *file* in the project, and the first cash-in of ADR-013's "the response is a pure function of the commit" |
| `backend/tests/fixtures/analyze_response_golden.json` | The whole `AnalyzeResponse` for the `REALISTIC` fixture repository, pretty-printed and compared byte for byte in `test_the_whole_response_matches_the_golden_file`. The second golden file, and the first over the *whole* wire contract rather than one field of it |
| `backend/scripts/smoke.py` | Real-network end-to-end check. **Not a pytest test, on purpose** (ADR-014) — `tests/conftest.py`'s socket block is a suite-wide guarantee, and a marker that lifted it would be an escape hatch for every future test. Prints counts only, never a specifier or a path |
| `backend/app/security/` | Complete for MVP scope. HMAC tokens are **deferred, not missing** — they belonged to `/api/source` / `/api/explain` (ADR-013) |
| `.claude/settings.local.json` | Local tool permissions, not source |
| `frontend/package.json` | Exact-pinned deps, no `^`: `cytoscape` 3.34.2, `react`/`react-dom` 19.2.8, `zod` 4.5.4, `zustand` 5.0.15; dev `vite` 8.2.2, `typescript` 5.9.3, `tailwindcss`/`@tailwindcss/vite` 4.3.3, `@vitejs/plugin-react` 6.1.1, `@types/react` 19.2.18, `@types/react-dom` 19.2.5. **No `@types/cytoscape`** — npm deprecates it as a stub; Cytoscape ships its own. **`mermaid` 11.17.2** as of 2026-09-01 (verified on npm before it was written in; also `latest`), for `src/ui/ComponentDiagram.tsx`. It is by a wide margin the largest dependency in the project: **159 transitive packages**, including **its own copy of `cytoscape`** — mermaid uses cytoscape for some diagram types, so the tree now carries two installs of it. `npm audit` reports 0 vulnerabilities. Worth naming against SECURITY.md's "Supply chain" section, which says the dependency count is "kept deliberately small" — it is less small than it was |
| `frontend/package-lock.json` | Committed |
| `frontend/tsconfig.json` | One file, `strict` plus `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, `noUnusedLocals`/`noUnusedParameters`, `verbatimModuleSyntax` |
| `frontend/src/api/schema.ts` | zod mirror of `backend/app/models/graph.py` + `api.py`, transcribed field for field: `extra="forbid"` → `strictObject`, `= None` → `.nullish().default(null)`, `Field(ge=0)` → `.int().min(0)`, each `AfterValidator(_within_*_limit)` → `.max(LIMITS.*)`. Covers `serviceMap`, `componentDiagram`, and the optional `node.description`. Deliberately does **not** assert `stats.dependencies === edges.length` — the pydantic models do not either |
| `frontend/src/api/limits.ts` | Hand-copied mirror of the response-shaped `Settings` limits. Duplicated on purpose; drift shows up as a rejected valid response, which is loud |
| `frontend/src/graph/elements.ts` | Wire graph → Cytoscape elements. `GraphNode.parent` becomes `data.parent` (a layout grouping hint since ADR-026, not a visible box — see below), and each file node also gets `tint`/`color`, a color key derived from its top-level path segment (`frontend/src/scene/directoryColors.ts`) for the per-file directory cue that replaced compound boxes. Drops a node's unknown `parent` and a dangling edge rather than letting `cy.add()` throw and take down the canvas; both are impossible per ADR-018, both are counted, and the count is all that is logged |
| `frontend/src/scene/directoryColors.ts`, `frontend/src/ui/Legend.tsx` | ADR-026: a fixed 8-color palette keyed off a file's top-level path segment, and an outside-the-canvas legend that lists every key present in the current response. Not yet unit-tested |
| `frontend/src/graph/fixture.ts` | A hand-written 10-node / 7-edge `AnalyzeResponse`, parsed through the schema at module load. Satisfies the *analysis* invariants too — component ordering, `sum(imports) == sum(importedBy) == edges.length == stats.dependencies`, `root.fileCount == stats.files` — so the canvas is exercised on the shape the builder actually emits. One description contains `<script>` on purpose. **`componentDiagram` was rewritten 2026-09-01**: it previously read `graph LR` with `src["src"]` / `api["src/api"]`, i.e. it used repository-derived strings as Mermaid **node identifiers**, which is exactly what ADR-024 guarantees never happens. It now matches what `app/analysis/component_diagram.py` actually emits (compare `backend/tests/fixtures/component_diagram_golden.mmd`) — the two `%%` header comments, `flowchart LR`, a `subgraph repo[…]`, synthetic ids `c0`/`r0`/`r1`/`ext`, and a `subgraph api["API surface"]` — and is derived from the fixture's own numbers rather than invented: all six files sit under `src`, so there is one real container holding 6 files, both `serviceMap` entries land in it, `stats.externalImports` is 5, and every import edge is internal to `src` so all of them collapse to a self-pair and none is drawn. A **second probe** sits in a `c1` label (`probe <script>alert(1)</script> <img src=x onerror=alert(1)>`), the diagram-shaped counterpart of the `<script>` in `utils.ts`'s description. That one label is a deliberate deviation from "derivable from the graph" and **cannot occur in a real response**, because `component_diagram._label` strips non-printables and removes twelve characters from every label — angle brackets, ampersand, double quote, hash, percent, backslash, backtick, both braces, the pipe, and the semicolon |
| `frontend/src/scene/GraphCanvas.tsx` | The Cytoscape instance. Built once per response; selection applied by toggling `selected`/`neighbor`/`faded` through a ref, so it re-runs neither React nor the layout. The layout is **not** passed to the constructor — see Broken / Known Issues |
| `frontend/src/scene/style.ts` | Stylesheet (colors as literals: Cytoscape paints to `<canvas>` and cannot read a CSS variable) plus `GRAPH_LAYOUT`. *(Corrected 2026-09-01: this used to say "currently built-in `cose`". ADR-026 replaced it with `cytoscape-fcose`; directory compound nodes are styled at zero background opacity and zero border width — a layout hook, not a visible box — and file nodes are colored by `data(color)`, the per-file directory tint.)* |
| `frontend/src/store/graphStore.ts` | Zustand: the validated response and a selected id. Nothing derived |
| `frontend/src/ui/App.tsx`, `Inspector.tsx` | Shell + status bar, and the inspector. **The first renderer of repository-authored text** — no longer the only one, see the next row. No landing page, no URL input, no loading state |
| `frontend/src/ui/ComponentDiagram.tsx` | The component-diagram panel: `AnalyzeResponse.componentDiagram` drawn via `mermaid` 11.17.2. **Nothing imports it** — it is deliberately not in `App.tsx`'s layout, so this is the render and not the layout decision, and the running app still shows only the canvas and the inspector. Configured `startOnLoad: false`, `securityLevel: 'strict'` (explicit though it is the default, because it gates mermaid's whole-SVG DOMPurify pass), `htmlLabels: false` at the **top level** rather than as the deprecated `flowchart.htmlLabels`, and `suppressErrorRendering: true` so mermaid does not draw its own error diagram into the document. `mermaid.render()` returns an SVG *string*, so the markup is **adopted, not injected** (ADR-025): parse as `image/svg+xml` into an inert document → refuse on an XML parse error or a non-SVG root → walk the inert tree and refuse any element outside a 15-element allowlist or any `on*` / `href` / `*:href` / `base` attribute → only then `document.importNode`. Refuse, not scrub: the failure is a blank panel with a message. Still **no `dangerouslySetInnerHTML` and no `innerHTML` anywhere in `src/`** |

No Docker files and no CI. **Frontend tests now exist, for one panel**: `frontend/vitest.config.ts`, `frontend/src/ui/ComponentDiagram.test.tsx` and `frontend/src/no-markup-sinks.test.ts` — vitest 4.1.11 + jsdom 30.0.1, **35 tests**, `npm test`, all passing. *(Corrected 2026-09-01: this line read "no Docker files, no CI, and **no frontend tests** — including for the panel above, whose browser evidence is manual and interactive". The panel above is precisely the part that now has coverage.)* Everything else in `src/` is still untested except by the repo-wide sink grep, and the suite runs under **jsdom, not a browser** — see the block below.

**Verified locally on 2026-09-01** (Node 22.23.1, npm 10.9.8):

- `npm install` clean, 0 vulnerabilities; `npm run build` (`tsc --noEmit && vite build`) green.
- Rendered in a real browser against the fixture: compound boxes nest `demo-service` → `src` → `api`/`lib`, seven edges drawn, ten nodes placed. *(Historical: this was true of the `cose` rendering on 2026-09-01. ADR-026, later the same day, stopped drawing compound boxes at all — see the style.ts row above.)*
- Clicking `src/lib/utils.ts` marks it `selected`, marks its three importers and their three edges `neighbor`, fades the other four elements, and leaves the enclosing directories unfaded. PRD §7's trace, confirmed by reading the applied classes rather than by looking at it.
- The `<script>` in that fixture description reaches the DOM as `&lt;script&gt;`, with zero `<script>` elements under `<aside>`.

**Also verified in a real browser on 2026-09-01** (Chrome, through the preview harness), for `src/ui/ComponentDiagram.tsx` and **for that panel only**. `npx tsc --noEmit` is green.

- **The element allowlist is an observed set, not a guess.** For `flowchart LR` with html labels off, mermaid 11.17.2 emits exactly 15 element types — `circle defs feDropShadow filter g linearGradient marker path polygon rect stop style svg text tspan` — identically for the golden fixture and for a diagram built entirely of hostile labels. The emitted SVG contained no `<script`, no `on*=` attribute, and no `foreignObject`.
- **Hostile labels, fed deliberately, and nothing executed in any case.** `<script>…</script>` → the label disappears entirely (mermaid's per-label DOMPurify deletes it); `<img src=x onerror=…>` → survives only as the literal characters `<img src="x">` inside an SVG `<text>`; `<svg onload=…>` → literal `<svg> </svg>`; `</text><script>…</script>` → disappears; `a < b > c & d` → literal text.
- **The fixture draws correctly**: one `<svg>`, 938×318, 5 nodes, 3 edges, 2 clusters, 8 visible text elements — matching the fixture diagram exactly. React 19 StrictMode's double effect invocation produces exactly one SVG; there is a cancellation guard.
- **All four refusal paths were driven, and each refused.** A `click c0 "javascript:…"` directive makes mermaid emit an `<a>` (href already dropped by mermaid's own sanitize-url) and the *element allowlist* refuses it, so no anchor entered the live DOM. A `click c0 href "https://…"` directive is refused by the *element allowlist and the attribute denylist*. Malformed Mermaid and an unknown diagram type both surface as a generic "the diagram could not be rendered", with the exception deliberately not shown — mermaid's parse errors quote the offending source line, and that line contains repository text. *(**Corrected 2026-09-01** by `ComponentDiagram.test.tsx`: this bullet claimed "each fired at its intended layer", and described the second directive as making "mermaid emit `xlink:href` with the `xlink` prefix undeclared, which is not well-formed XML, so the *XML parse* refuses it first". Mermaid 11.17.2 emits a plain `href` on an `<a>` — no `xlink:href`, no `xmlns:xlink`, confirmed against the raw serialization under `securityLevel: 'loose'` — so the document is well-formed and the XML gate never fires. Observed refusal: `unexpected <a> in the rendered diagram`. Security-neutral; ADR-025 carries the full correction.)*
- **Two discoveries worth carrying**, both about mermaid rather than about us. (1) Mermaid's html-label output is **not well-formed XML** — a foreignObject carrying `<img src="x">` fails to parse, "Opening and ending tag mismatch: img" — so the XML parse is a hard structural gate on `htmlLabels: false`: flip that config back on and the panel goes blank and says so instead of quietly growing an HTML sink. (2) Mermaid's `sanitizeMore`, the half that escapes `<` → `&lt;`, is **gated on html labels being ON**, so with them off it is a no-op; what runs unconditionally is the outer `DOMPurify.sanitize(text, {FORBID_TAGS:['style']})`. The protection is DOMPurify, **not** entity-escaping, and describing this panel as relying on mermaid escaping angle brackets would be wrong.
- **What this does not cover.** The panel was driven from `fixture.ts`, never from a server response. *(Corrected 2026-09-01: this bullet also said the verification was "**manual and interactive** — there are no frontend tests, so nothing will catch a regression in any of the above. The evidence exists; it is not repeatable." That is now false — see the block immediately below.)*

**Automated later the same day, 2026-09-01** — `frontend/src/ui/ComponentDiagram.test.tsx` and `frontend/src/no-markup-sinks.test.ts`, **vitest 4.1.11 + jsdom 30.0.1, 35 tests, `npm test`**, all passing. This is the automation the block above called for, and it is worth reading its limits as carefully as its coverage.

- **What it covers.** Mermaid's real output for the hostile-label fixture (no `<script>`, no `on*`, no `foreignObject`); the panel's live DOM after mounting; all four refusal paths plus the not-an-SVG path, each isolated by stubbing `mermaid.render`'s return value so the exact refusal message can be pinned rather than depending on which check happens to fire first; that a mermaid parse failure surfaces only the fixed message and never the exception — with an assertion that mermaid *does* quote repository text in its parse errors, so the fixed message is load-bearing rather than decorative; the `htmlLabels: false` and non-`loose` `securityLevel` controls, observed through their effects rather than by reading config back; and that no file in `src/` uses `dangerouslySetInnerHTML` / `innerHTML` / `outerHTML` / `insertAdjacentHTML` / `document.write`.
- **What it does not.** **No real browser is driven.** This is jsdom's parser and DOMPurify's behaviour under it, not Chrome's. Two SVG-metric stubs (`getComputedTextLength`, `getBBox`) are installed because jsdom implements no SVG text metrics; both move geometry only, and nothing the suite asserts is geometric. Do not read these 35 tests as browser equivalence — they are a regression net over the same claims, in a different engine.
- **It found a real error in ADR-025** on its first run: the `click … href` mechanism, corrected in the bullet above and in that ADR.
- **One parse5 conformance gap is patched, and the next person will hit it.** The suite adds a single row, `fedropshadow` → `feDropShadow`, to parse5's `SVG_TAG_NAMES_ADJUSTMENT_MAP`. parse5 8.0.1 — jsdom's HTML parser — carries **36** of the 37 rows in the WHATWG HTML spec's "adjust SVG tag names" table and is missing exactly that one; verified locally against the installed package, whose key set is the spec table's minus that row with no extras. Mermaid emits `<feDropShadow>` in **every** `theme: 'dark'` flowchart (inside its node-shadow filter), and mermaid's own DOMPurify pass reparses the serialized SVG as HTML — the HTML parser lowercases foreign element names unless that table restores them. So under stock jsdom the element arrives as `fedropshadow`, misses `ALLOWED_ELEMENTS`'s camelCase `'feDropShadow'`, and the panel refuses **every** diagram, while a real browser draws it. **Without the patch a correct component looks broken**; the first version of the suite reported exactly this as a production bug. The patch is asserted rather than assumed — one test checks the gap was really there and that closing it changes the parse.
- **Open, and it is the assumption the suite hangs from.** The `feDropShadow` behaviour has **not** been re-confirmed in a real browser since the suite was written. Every element-allowlist assertion rests on Chrome implementing the full table and restoring the camelCase name — which the spec requires, and which the interactive session above implied by rendering at all, but which nobody has checked deliberately. Recorded in TODO.md as a cheap open item rather than quietly assumed.

**Verified locally on 2026-09-01** (Python 3.14.7, uv 0.12.3):

- `uv sync` resolves and installs cleanly; the project installs as an editable package.
- `uv run pytest` → **1431 passed** in ~13s. `uv run mypy` (strict) → clean over 53 files (`scripts/` and `tests/` are in scope). `uv run ruff check .` → clean. (`ruff format` is still not a gate — see the issue below.)
- **Re-verified the same day, after ADR-008 and the response golden file landed**: `uv run pytest` → **1458 passed**, `uv run mypy` (strict) → clean over 55 files, `uv run ruff check .` → clean.
- `frontend/`: `npm ci` installs clean with **0 vulnerabilities**; `npm run build` (`tsc --noEmit && vite build`) succeeds. One warning, not an error — the bundle is 720 kB (224 kB gzipped) because Cytoscape ships in the main chunk and nothing is code-split yet.
- `uvicorn app.main:app` imports and builds the application; `GET /api/health` returns `{"status": "ok"}`. **Not yet run against a real repository over HTTP** — the smoke script still calls the pipeline directly.
- tree-sitter ABI spike printed **`14`**, and `QueryCursor` imports successfully alongside `Language`, `Parser`, and `Query`.
- The `progress_callback` half of the spike is now done too, and the answer is **it does not work** — see ADR-010 and the issue below.

Two `pyproject.toml` corrections were needed: `[tool.ruff] src = ["."]` (it was `["app", "tests"]`, which pointed *inside* the package so isort never treated `app` as first-party), and `S105`/`S106` added to the test per-file-ignores because redaction tests must hardcode fake credentials.

## In Progress

- **`frontend/` — wired to the real backend, not finished.** The landing page, URL input, loading state, and `src/api/client.ts` landed 2026-09-01 (see Recently Completed). What is still missing from the Day 2 list: hover, search, and collapse/expand. None of it is blocked; it is simply not written. *(**Corrected 2026-09-01**: this bullet used to also list "the component-diagram panel (`mermaid` is not even a dependency yet), the service-map panel". All of that is now false — `mermaid` is pinned at 11.17.2 and both `src/ui/ComponentDiagram.tsx` (browser-verified, ADR-025) and `src/ui/ServiceMap.tsx` exist. What is true instead is smaller and worth saying separately: **nothing imports either panel**, so both are absent from the running app until someone puts them in `App.tsx`'s layout, and there are **still no frontend tests**, which is now the sharpest edge on this file — the diagram panel's whole safety argument rests on a browser session nobody can replay.)* *(**Corrected again 2026-09-01**, later the same day: the frontend-tests half of that note is closed. 35 tests exist and the diagram panel's safety argument is re-run by `ComponentDiagram.test.tsx` on every `npm test` — under jsdom, not a browser. The unwired-panels half stands, and is now the sharpest edge here.)*
- Nothing else is under active implementation. **The backend is joined end to end**: the router and the component-diagram generator were built in parallel on 2026-09-01 and the seam between them was closed on merge, so `/api/analyze` now returns a populated `componentDiagram` and every module in `app/analysis/` has a caller except `path_safety.py`. The diagram is built from the **capped** `nodes`/`edges` the response carries rather than from the uncapped analysis — a test pins that, because the other way round draws containers for files the client cannot see. Next: put the two written panels in the layout, then CORS / deploy (Day 3).

## Broken / Known Issues

- No CI yet.
- **The dev server reaches the backend through a proxy, not CORS, because the backend has none yet.** `POST /api/analyze` carries a JSON body, so a cross-origin `fetch` triggers a preflight; with no `Access-Control-Allow-Origin` anywhere in `app/api/` (CORS is still Day 3 — ADR-008 covers the rate limiter and concurrency gate, not CORS, and has landed), that preflight fails. `frontend/vite.config.ts` proxies `/api` to `http://localhost:8000` so the browser's request stays same-origin in dev, and `src/api/client.ts` defaults its base URL to the empty string so it hits that relative path. A deployed frontend has no dev proxy, so `VITE_API_URL` plus real CORS on the backend are both required before Vercel+Railway/Render/Fly can work, not just one of them.
- ~~**The frontend has no tests, and its zod schema has never seen a server response.**~~ **Closed 2026-09-01, for the schema half.** `src/api/client.ts` posts real repository URLs (`sindresorhus/p-limit` among them) through the dev server to a live backend and `AnalyzeResponseSchema.safeParse` accepted the response unmodified — the hand transcription of `graph.py`/`api.py` was correct on the first real request. ~~**The frontend still has no test suite**~~ — **closed 2026-09-01, later the same day.** A test suite now exists: vitest 4.1.11 + jsdom 30.0.1, 35 tests, `npm test`. It went to exactly the place this bullet said it should, `src/ui/ComponentDiagram.tsx`, and it immediately found a factual error in ADR-025's account of the `click … href` refusal path — which is the argument for regression tests over recorded observations, made concretely.

  **What remains of this item is the schema half, unchanged.** The two drift risks named here originally — `frontend/src/api/limits.ts`'s hand-copied `Settings` values and field optionality — are still uncovered; they simply have not been observed to drift yet. The golden-file `AnalyzeResponse` (ADR-013, Day 1) exists on the *backend* side as `backend/tests/fixtures/analyze_response_golden.json`, and **sharing it with the frontend suite is now a small job rather than a blocked one**, because the runner it needed is in place. That is the highest-value next frontend test. *(The bullet used to continue: "**The missing test suite got sharper on 2026-09-01, not milder** … every one of those claims was established by driving a browser by hand. Nothing re-runs it." Something re-runs it now.)*
- ~~**The frontend layout is a placeholder and looks it.**~~ **Closed 2026-09-01, later the same day** (ADR-026). `cose`'s compound-box rendering was measured against a real repository (`expressjs/express`, 141 files/153 edges) rather than the ten-node fixture this bullet asked for, and the finding was sharper than "pick a different layout": directories nested as deep as 5 levels of padded boxes reduced files to near-invisible dots. `cytoscape-fcose` replaces `cose`; directory compound nodes are demoted to a zero-footprint layout hook, and a per-file color tint (`directoryColors.ts`) plus an outside-the-canvas legend (`Legend.tsx`) replaced the visible box as the directory-membership cue. `cola`/`elk`/`dagre` were considered and rejected in ADR-026's Alternatives.
- **Cytoscape must not be given a layout at construction time in this app, and the reason is not obvious.** React commits the canvas subtree and runs the effect before the surrounding flex chain has a resolved height, so the container measures 0×0, Cytoscape sizes its canvases to zero and lays the graph out against an empty viewport, and nothing ever appears — with no error anywhere. `GraphCanvas.tsx` therefore constructs the instance without a layout and runs it from the first `ResizeObserver` callback that reports a real box. This was found by inspecting canvas dimensions in a live page, not by reading the code, and it will silently come back if someone "simplifies" the constructor call. *(Corrected 2026-09-01: the refit half had a real, separate bug, fixed alongside ADR-026 — `cy.resize()`/`cy.fit()` ran synchronously against a `clientWidth`/`clientHeight` read that could still reflect the pre-resize box, leaving the graph stuck shrunk after a later viewport resize. Fixed by reading the observer entry's `contentRect` and deferring the refit to `requestAnimationFrame`; see ADR-026's Consequences.)* *(**Corrected 2026-09-02**: "runs it from the first `ResizeObserver` callback that reports a real box" was the whole trigger, and that was a bug — **a hidden document is delivered no `ResizeObserver` callbacks at all**, so a canvas mounted in a background tab never laid out and rendered Cytoscape's default `grid` instead. The constructor half of this entry still holds and must not be "simplified"; what changed is that the layout now also runs immediately and synchronously when the container already has a box, with the observer kept as the fallback for the 0×0 case. Do not re-route the initial layout back through a `ResizeObserver` or `requestAnimationFrame` — neither is delivered while hidden. See ADR-027's second correction.)*
- **`width: 'label'` is deprecated in Cytoscape 3.34 with no replacement enum.** File nodes are therefore a fixed 11 px dot with the name set beside them rather than a box sized to its text. That is also the denser read, but it is a workaround first.
- **Resolution runs outside the deadline, and the bound on it is a count rather than a clock.** `resolve_imports` and `build_graph` both run *after* `analyze_repository` has spent its 60 s budget, and neither takes a `Deadline` — that part is unchanged and deliberate (ADR-019: a clock there makes output non-deterministic and a partial resolution indistinguishable from a genuinely unresolvable import). What *has* changed is the input. `resolver.py` used to justify the omission by pointing at `MAX_SOURCE_FILES`, which caps files and not imports *per* file, so the real ceiling was `MAX_EXTRACTED_BYTES` (256 MiB): measured 2026-08-31 at **1 002 000 imports / 76.1 s to resolve / 81.8 s total**, more than the whole analysis budget, off a 21.7 MiB fixture. **`MAX_IMPORTS` (100 000) now caps the running import total in the pipeline**, re-measured on the same fixture at **7.7 s / 8.2 s** with `truncated` and `imports_truncated` both set. The residual is worth knowing: the worst case is now ~60 s parse **plus** ~8 s resolve — bounded and known, not free — and raising `MAX_IMPORTS` spends time in a phase with no clock. The graph builder was never the problem (300 000 imports build in 0.22 s, linear).
- ~~**Nothing bounds *concurrent* analyses.**~~ **Closed 2026-09-01** (ADR-008). `app/api/rate_limit.ConcurrencyGate` now enforces `MAX_CONCURRENT_ANALYSES` and `SlidingWindowLimiter` enforces both `RATE_LIMIT_ANALYZE` windows, both checked by `app/api/routes.analyze` before a worker thread is spawned. This is why ADR-023 declined an `asyncio.wait_for` rather than reaching for one: a timeout that abandons a running thread makes an overloaded server *worse*, and shedding load was always meant to be this gate's job, not the timeout's.
- **`stats.truncated` has three producers and does not say which fired.** `MAX_SOURCE_FILES`, `MAX_IMPORTS`, and now the graph caps all set it, and `Stats` is a frozen wire contract the frontend mirrors, so no field distinguishes them. Deliberate — a consumer's response to all three is the same — but an operator debugging a short graph has to read the server log line, which carries the emitted counts and the configured caps side by side.
- **A capped graph reports counts over what it emitted, not over the repository.** `stats.externalImports` on a capped response counts only surviving files, and a directory whose children all fell past the cut survives with `fileCount` 0. Both are deliberate (ADR-023) and both are pinned by test: the alternative is a total that no set of nodes in the response adds up to. Worth knowing before anyone reads a truncated response as a description of the repository — `truncated` is the signal that it is not.
- **The frontend caps nodes and edges again, and the two caps must not drift.** `frontend/src/api/schema.ts` bounds `nodes`/`edges` at its own copy of `MAX_NODES`/`MAX_EDGES` in `limits.ts`. That was written when the server enforced neither and was the only thing binding them; now that ADR-023 enforces both server-side it is ordinary defence in depth, and the two agree. But they are agreeing by hand: if anyone raises a cap in `app/config.py` without editing `limits.ts`, a legal response becomes a **blank screen** rather than a large graph, because zod rejects the whole document. The real fix is generating `limits.ts` from `Settings` rather than transcribing it.
- **The compression-ratio guard and the 50 000-member cap are in tension at the extreme.** An archive of 50 000 *empty* files is mostly zero padding: measured at ~25 MiB extracted from ~355 KiB compressed, a ratio of ~74 against a cap of 100. It passes today, but with under a 1.4× margin that no real repository approaches — ordinary source sits around 5:1. The 50 000-member test therefore lifts the ratio guard, so it tests the count cap alone rather than becoming a hostage to the zlib version. If a legitimate repository is ever refused as a bomb, this is the interaction to look at first; the fix is to raise `RATIO_FLOOR_BYTES` or to exclude header padding from the numerator, not to raise the ratio.
- **Member paths are never Unicode-normalized, and downstream code must not normalize them either.** `archive.py` compares the exact code points the archive carried, which is the safe direction — NFKC folds U+FF0E to `.` and U+FF0F to `/`, so normalizing would turn an inert component into `../etc/passwd`. Six tests pin it. But the guarantee stops at this module: a normalizing filesystem, a `unicodedata.normalize` before a comparison, or a database collation would reintroduce the traversal, and folding NFC/NFD would also break the `/api/source` token, whose subject is the yielded path. Compare yielded paths byte-for-byte.
- **tree-sitter 0.26.0's `progress_callback` is broken, and the plan depended on it.** With a `bytes` source it is silently ignored (plus a `UserWarning` that `filterwarnings = ["error"]` turns into a test failure); with the chunked-reader source form it **segfaults the interpreter** as soon as it fires, C stack in `PyObject_IsTrue` inside `_binding`. `timeout_micros` was removed in an earlier release, and 0.26.0 is the newest version on PyPI, so there is no upgrade to reach for. **There is no in-parse timeout available at all** — the `Deadline` cannot preempt a parse or a query already running, and only stops the *next* unit of work. Bounded structurally instead (ADR-010). Re-test both source forms before anyone reinstates it.
- **The parser's real hazard is the query, not the parse.** tree-sitter's query engine is quadratic in the *width* of an ERROR node. A 1 MiB file of `(` parses in 0.23 s and then takes ~11 minutes to query; measured 0.09 / 0.95 / 3.97 s at 10k / 40k / 80k children. Every pattern costs the same, so it is the traversal, not the pattern — and nesting depth is *not* the trigger (20 000 levels query in 4 ms). `_is_pathological` refuses that shape. Worst case across a 21-input hostile sweep is now ~3.3 s per file, which is parse time. **A single file can still occupy a worker for a few seconds**, and the 60 s deadline stops the next file rather than the current one.
- **A shebang hides a file's header comment.** `#!/usr/bin/env node` is not one of the three comment forms ADR-013 names, so the description scanner stops at the `#` and a CLI entry point gets none even when the very next line is a good JSDoc header. This is a gap rather than a decision: the fix is to skip one leading `#!` line, it is a few lines in `app/analysis/descriptions._leading_comment`, and it was left out to keep the extractor exactly the three documented forms. A test pins the current behaviour so it is deliberate. Low impact — typically one or two `bin/*.js` files per repository.
- **`/// <reference … />` becomes a useless-but-accurate description.** A TypeScript file whose first line is a triple-slash directive gets `<reference types="node" />` as its description, because that *is* its leading comment. Not special-cased on purpose: the rule "quote the header comment" survives better without an exception list, and the output is unhelpful rather than wrong. Cosmetic.
- **A comment made entirely of undecodable bytes yields a description of one U+FFFD.** The bidi/format characters are dropped and the character the scan window cut in half becomes a replacement character, so a legacy Shift-JIS comment can produce a one-character description. Honest — the file really does have a comment we cannot read — and bounded. Dropping an all-U+FFFD result was considered and declined as a heuristic; "absent but never wrong" does not require "absent whenever ugly".
- **A description is bounded, but it is still repository-authored text on a record ADR-016 calls content-free.** The invariant holds as *reasoned* — nothing on `SourceFile` scales with the size of a file, and 500 characters is a constant — but note that the test which enforces it, `test_the_analysis_carries_no_file_content`, only forbids `bytes`/`bytearray` and would happily pass with a whole megabyte source file in a `str`. `test_the_only_repository_text_carried_is_a_bounded_description` was added beside it to assert the bound directly. Anyone adding a second text field to `SourceFile` should extend that second test, not lean on the first.
- **`path_safety.py` still has no caller.** It is correct and mutation-tested and protects nothing, because under ADR-003 nothing writes to disk. `secret_filter.py`'s first caller landed on 2026-08-31 (`analysis/pipeline.py`); its second is a serving-time endpoint, and under ADR-013 that is `/api/source` (post-MVP), **not** `/api/explain`, which no longer exists — so its SECURITY.md row stays `Partial` for the whole sprint rather than flipping on Day 2 as previously planned. Grepping for a module and finding it is not evidence that it runs — grep for the *call*.
- ~~**`.mts` and `.cts` are not analyzed.**~~ **Closed 2026-09-01.** The analyzed set is now eight extensions — `.tsx .js .jsx .mjs .cjs` → TSX, and `.ts .mts .cts` → TypeScript — `resolver.EXTENSIONS` names the same eight, and `import './x.mjs'` now reaches an `x.mts` sitting next to it. See Recently Completed. What is still absent, and still deliberate: `.mjs` does **not** fall back to `.ts`, and `.d.ts` is not a separate case (a `.d.ts` file's suffix is `.ts`, so it is parsed as an ordinary TypeScript file and becomes its own node — untested and unconsidered either way, which is worth knowing before anyone reads the eight-extension set as complete).
- **Route detection misses four real route shapes, each pinned by a test.** `router.route('/x').get(h)` splits the path and the verb across two calls; Fastify's `fastify.route({method, url})` object form puts both in an object literal; NestJS decorators need class-level `@Controller` prefix joining; and the Next.js Pages Router's default-export handler declares no method at all. All four are deliberate omissions under ADR-021's "absent beats invented" — a short service map is trustworthy, an invented URL is not — and all four are on the Deferred list rather than forgotten. The visible symptom: a repository that registers every route through `router.route(...)` chains gets an empty service map with no indication why.
- **A route summary is repository-authored text on `SourceFile`, which ADR-016 calls content-free.** Same standing as `description` and bounded the same way — `MAX_ENDPOINT_SUMMARY_CHARS` (300) per summary and `MAX_SERVICE_ENDPOINTS` (200) across the whole analysis, so the total is a constant and does not scale with any file. Worth knowing that `test_the_only_repository_text_carried_is_a_bounded_description` was written for `description` alone and does **not** cover `routes`; anyone adding a third text field should extend it rather than assume it generalizes.
- **The resolver is case-sensitive, so a file with an uppercase extension is unreachable.** `Util.TS` is a node — `pipeline.py` lowercases the suffix to pick a grammar — but `./Util` does not resolve to it, because the resolver's candidate extensions are lowercase literals and nothing is folded. The fix is *not* to fold: `fetch/archive.py` guarantees byte-exact member names, folding NFC/NFD or case could collapse two genuinely distinct files onto one node, and the whole Unicode-normalization issue above says why comparisons here must stay exact. Two tests pin the current behaviour so it is deliberate. If it ever matters, the honest fix is to generate cased candidate variants, not to fold the set.
- **Truncation past `MAX_SOURCE_FILES` reports no count of what it dropped.** Reaching the cap abandons the generator, which abandons the download — deliberately, since the cap exists to bound work. `truncated` is therefore the only honest signal; a "files not analyzed" number would require reading the rest of the transfer to produce.
- **Parser-level skips are invisible to the pipeline, so they are absent from the skip counts.** `parse_source` reports a skipped file — oversized, binary, pathologically malformed — by returning `None`, and the pipeline treats that the same as a file with no imports and no routes. *(ADR-021 narrowed this slightly: the pipeline can now at least see the `None`, where before a skip was indistinguishable from an empty yield. It still does not count it, so the behaviour below is unchanged.)* Such a file therefore stays in the analysis as a node with its real bytes and line count and zero imports. That is honest (it *is* a real file) but it means `skipped` counts only the archive reader's drops, secret-filtered paths, and unsupported extensions. Closing it means changing `extract_imports`' signature to report skips; a test pins the current behaviour so it is deliberate rather than incidental.
- **The real-GitHub run has happened, but only on the happy path.** `backend/scripts/smoke.py` analyzed three real repositories on 2026-08-31 (see Recently Completed), so codeload responses, redirect shapes, chunk sizes, and timing are no longer unverified. What is still unverified is everything adversarial: a real repository is not hostile, so no security control was *triggered*. The archive guards, the SSRF checks, and every resource cap remain fixture-only. A guard that wrongly rejects a legitimate repository would now be caught; a guard that fails to stop an attack would not.
- **Two parallel implementations of the pipeline existed, and the merge kept one of each half.** PR #2 supplied the code, PR #3 the documentation, so several doc claims described a module that was never merged (`iter_bytes`, `ArchiveRoot`, `grammar_for`, `Ingested`) and `archive.py`/`pipeline.py` were left citing ADR numbers that had come to mean something else. Corrected on 2026-08-31; see the numbering note in DECISIONS.md. **If a doc and the code disagree here, trust the code** — that is CLAUDE.md's rule and this is the case that proves why.
- **`assert_public_ip` narrows DNS rebinding, it does not close it.** The connection that follows is made by name, so a resolver that answers differently the second time is not caught. Closing it needs connect-by-IP with SNI, which v1 does not do. Recorded in `docs/SECURITY.md`.
- **`ruff format` is not a project gate — do not run it.** `uv run ruff check .` is the gate and is clean. `ruff format --check` reports 5 of 21 files as unformatted: four pre-existing (`app/logging_setup.py`, `tests/test_config.py`, `tests/test_logging.py`, `tests/test_models.py`) and `app/security/net_guard.py`, which is unformatted for the same reason they are — the formatter wants to join wrapped constructs into lines that then exceed the configured `line-length = 100`. Running it would rewrite unrelated code to no benefit. Either adopt it repo-wide as a deliberate decision or leave it alone; do not apply it to one file.
- ~~The contract layer is still unexercised by any *route*.~~ **Closed 2026-09-01** — `tests/test_api_routes.py` drives real repositories through the real application. The tuple/`list` seam that `test_the_output_composes_into_an_analyze_response` predicted was real and is handled where it said it would be, in `app/api/routes._analyze_blocking`. `repository` and `serviceMap` are now produced and asserted. *(**Corrected 2026-09-01**: this line used to end "**`componentDiagram` is still only as good as its documentation**, because nothing writes it." That was already false when written — `app/api/routes.py` calls `build_component_diagram` on the capped nodes and edges — and it is false twice over now that `frontend/src/ui/ComponentDiagram.tsx` renders the field. The field is produced, asserted over a whole response, and drawn.)*
- `pytest.filterwarnings = ["error"]` now runs against a real suite and is clean; no targeted ignores have been needed.
- The pydantic **mypy plugin is not enabled** (no `plugins` key in `[tool.mypy]`). Constructor type-checking still works via PEP 681 `@dataclass_transform` on pydantic's metaclass. Reviewed and judged unnecessary; do not assume the plugin is present when reading type errors.

## Recently Completed

- **2026-09-02** — **The graph layout never ran in a hidden tab, and had been rendering Cytoscape's default grid instead (ADR-027's second correction).** Reported as "the distribution looks even worse now" with a screenshot of a regular lattice. `GraphCanvas.tsx` started the initial layout only from its `ResizeObserver` callback, and **a hidden document receives no `ResizeObserver` callbacks at all** — not even the initial one `observe()` queues. Paste a URL, switch tabs during the 7 s analysis, come back: the canvas mounted hidden, fCoSE never ran, and Cytoscape's default **`grid`** layout was what rendered. It never self-corrected, because on return the size had not changed. Proven rather than guessed: the bounding box measured exactly `467x748` **identically across reloads and unchanged by flipping `randomize`**, which cannot happen for a randomized force layout; a fresh `ResizeObserver` on a 368x767 container fired zero times in 1.5 s with `document.hidden === true`. Fixed by laying out immediately and synchronously when the container already has a box, keeping the observer only as the fallback it was written for; the overlap/camera step was un-deferred from `requestAnimationFrame` for the same reason. Verified hidden *and* visible with matching numbers (core-area fraction 0.512 vs 0.511, 0 overlaps, hub centred). **Separately, `gravity` had been scaled in the wrong direction** — easing it down to let a large graph "breathe" is what produced the sparse, stringy sprawl; raising it on a 141-node repository lifted the core-area fraction from ~0.32 to ~0.43 and halved the area, so it now runs from the opposite end of the range. **Three alternatives were measured and rejected**, all worse: dropping compound nesting from the layout (great on express, disastrous on astro — core 0.72 → 0.134), flattening to top-level directories only (core 0.123), and `randomize: false`, which does not seed deterministically but starts from the nodes' current positions — all (0, 0) on a fresh graph — producing a partial layout that never emits `layoutstop`, silently disabling separation and the camera.

- **2026-09-02** — **Node overlap is now a solved constraint rather than a tuning hope, and the canvas stopped swallowing drags (ADR-027).** Three reported defects, one pass. `frontend/src/scene/overlap.ts` (new, **tested** — `overlap.test.ts`, 9 tests, the first for any `scene/` module, suite now 43) separates nodes by their *rendered footprint* — dot plus the label drawn to its right, which is the collision that actually reads as crowding — deterministically, once, between fCoSE settling and the camera being aimed. Measured in a browser: `expressjs/express` (141 file nodes) reaches **0** overlapping pairs in ~10 ms; `withastro/astro` (2,953) goes **171,600 → 5,627** in ~960 ms. The second is a 97% reduction, not a guarantee, and the module says so — reaching zero there needs the layout scaled ~3x, which sprawls it to 14,917 units. This also **reverses part of ADR-026's correction from earlier the same day**: raising fCoSE's force constants was measured to be counter-productive (12000/110 grew express's bounding box to 4305x4465, halving its fit zoom, and *still* left 14 overlaps), so they settle at 90/8000 and separation is handled by the constraint pass instead. Alongside: **edges and directory compound nodes are now `events: 'no'`** — a drag starting on a hairline edge did nothing instead of panning, and a compound node's body is the whole invisible box its children sit in, so dragging apparent empty canvas silently moved a whole subtree; box selection is off and directories are `ungrabify()`d as well. `wheelSensitivity` 0.3 → 0.8. The tests earned their keep immediately, catching a float-equality fixed point (exactly-touching boxes reporting a ~1e-15 overlap forever), a stall detector keyed to the wrong metric, and two of their own assertions being **vacuous** — constructing Cytoscape with `elements` and no `layout` runs the default *grid* layout and discards the positions under test.

- **2026-09-02** — **The backend's real size ceiling was measured, and it is not the one the constants suggest.** Prompted by "can it open Node.js and React?". It cannot, and the binding limit is `MAX_REPO_API_SIZE_KB` (256 MiB) checked against the GitHub repos API's `size` field — which reports the **server-side git repository including all history**, not the working tree or the tarball. So the gate fires on history, not on how much code there is to analyze: `microsoft/TypeScript` is refused at 2,792 MiB of history despite a 24 MiB tarball, and `vercel/next.js` at 2,493 MiB despite 48 MiB. `MAX_DOWNLOAD_BYTES` (64 MiB) and `MAX_NODES` (6,000) are never reached first for any repository tried. Measured: `nodejs/node` 1,508 MiB → 413, `react/react` 1,054 MiB → 413, `microsoft/TypeScript` 2,792 → 413, `vercel/next.js` 2,493 → 413; `withastro/astro` 229 MiB → **200 in 7 s** (2,953 files, 4,376 nodes, 3,578 edges) and `sveltejs/svelte` 119 MiB → **200 in 3 s** (3,000 files, `truncated: true`). Two findings worth acting on separately, both in [TODO.md](TODO.md): the limit is a **poor proxy** for analysis cost, and a **renamed repository reports the wrong error** — `facebook/react` now 301-redirects to `react/react`, and since the client correctly refuses to follow redirects (SSRF policy, ADR-009) the user is told "GitHub could not be reached" rather than that the repository moved.

- **2026-09-02** — **ADR-026's "directories are not drawn" guarantee did not actually hold while a node was selected, and three canvas complaints were fixed together.** Reported from screenshots of a real session against `expressjs/express`. The selection highlight "glitch" was not a state-machine bug — the selection effect already recomputed all four classes from scratch — it was **stylesheet rule order**: `GraphCanvas.tsx` classed `node.ancestors()` (a file's enclosing *directory* chain) as `neighbor`, and `node.neighbor`'s `border-width: 2` / `overlay-opacity: 0.12` sit after `node.directory`'s zeroed rule, so every selection repainted the invisible compound boxes as bordered, blue-washed rectangles across the canvas. Fixed at both layers: selection classes are now scoped to `cy.elements().difference(cy.nodes('.directory'))` (the `ancestors()` union is dropped — it existed only to keep a *drawn* box from fading), **and** `style.ts` appends a final `node.directory` rule zeroing background/border/overlay, so the guarantee no longer rests on one call site's element arithmetic. Alongside it: the **opening camera** now fits and then, if that zoom is below `INITIAL_ZOOM.MIN`, zooms to the floor and centers the highest-degree node (the entry point on a real repository) instead of framing the whole graph at an unreadable zoom; **stale hover** is fixed by making `hovered` a singleton and clearing it on the container's `pointerleave`, since `mouseout` is not guaranteed to pair with every `mouseover`; and fCoSE's `idealEdgeLength`/`nodeRepulsion` went up and `gravity` down to loosen node density. **Verified in a real browser**, not jsdom, by reading computed Cytoscape styles across select-hub → select-other → select-hub-again → deselect: zero directory nodes carry a state class and zero have a non-zero border/overlay/background in any state, including when `neighbor`/`selected` are forced onto one by hand. The 35 existing frontend tests still pass; `tsc --noEmit` is clean. No new ADR — recorded as a dated Correction under ADR-026, whose consequence list asserted the thing that turned out to be false.

- **2026-09-01** — **Directory compound boxes stopped being drawn, and the layout engine changed with them (ADR-026)**. Measured against a real repository for the first time (`expressjs/express`, 141 files/153 import edges) rather than the ten-node fixture, `cose`'s nested compound boxes were found to nest as deep as 5 levels, dominate the canvas, and reduce files to near-invisible dots — a rendering defect, not an analysis one. `cytoscape-fcose@2.2.0` replaces `cose`; directory compound nodes remain in the Cytoscape element set (`GraphNode.parent` → `data.parent` unchanged) purely as an invisible fCoSE layout hook, and directory membership is now conveyed by a per-file color tint (`frontend/src/scene/directoryColors.ts`, an 8-color fixed palette keyed to a file's top-level path segment) plus an external legend (`frontend/src/ui/Legend.tsx`), both new files and both untested so far. `elements.ts` gained the `tint`/`color` fields that carry this. A real, independent bug was fixed alongside it in `GraphCanvas.tsx`: the `ResizeObserver` refit could compute `cy.fit()` against a stale `clientWidth`/`clientHeight` read mid-reflow, leaving the graph stuck shrunk after a later viewport resize — fixed by reading the observer entry's `contentRect` and deferring the refit to `requestAnimationFrame`. `docs/ARCHITECTURE.md`'s Rendering and Layout sections and `docs/TODO.md`'s layout item are updated to match.

- **2026-09-01** — **The frontend got a test runner, and it corrected an ADR on its first run.** `frontend/vitest.config.ts`, `frontend/src/ui/ComponentDiagram.test.tsx`, `frontend/src/no-markup-sinks.test.ts`; **vitest 4.1.11 + jsdom 30.0.1** added to `frontend/package.json` (both checked against the registry, not recalled), plus `parse5` 8.0.1 pinned as a devDependency so the suite patches the same parser instance jsdom uses rather than a second copy. **35 tests, `npm test`, all passing.** This closes the item TODO.md called the highest-value frontend test, and it earned that billing immediately: it **falsified a factual claim in ADR-025** — a `click c0 href "https://…"` directive emits a plain `href` on an `<a>`, not the `xlink:href` the ADR described, so the XML-well-formedness gate is not what refuses it; the element allowlist and the attribute denylist are, and the refusal still holds. **Security-neutral, and corrected in five places rather than quietly fixed**: ADR-025 gained a dated Correction section, and the same wrong mechanism was removed from `ComponentDiagram.tsx`'s header comment, docs/SECURITY.md's XSS row, this file's verification block, and docs/TODO.md. The suite also surfaced a parse5 conformance gap that makes a *correct* component look broken under jsdom — `fedropshadow` → `feDropShadow` is missing from parse5 8.0.1's SVG tag-name adjustment table (36 of the spec's 37 rows present) while mermaid emits `<feDropShadow>` on every dark-theme render; documented in ADR-025's Correction and in the verification block above so it is not rediscovered from scratch. **Two limits stated plainly rather than buried**: this is **jsdom, not a browser**, so it is not evidence about Chrome; and one panel is covered — `App.tsx`, `GraphCanvas.tsx`, `Inspector.tsx`, `ServiceMap.tsx`, the store and the client still have no tests of their own. No new ADR: this implements a TODO item and corrects an existing decision's record, it does not decide anything.

- **2026-09-01** — **The rate limiter and concurrency gate landed (ADR-008)**, closing the sharpest resource gap named repeatedly throughout this document. `app/api/rate_limit.py` (`SlidingWindowLimiter`, `ConcurrencyGate`, ~65 lines together) is checked by `app/api/routes.analyze`: the per-IP window before `parse_github_url` runs, the concurrency gate immediately before the worker thread would be spawned. `RateLimitedError` gained the one per-instance argument any `AppError` carries (`retry_after_s`, feeding a new `headers()` hook on the base class) — it is a wait time, never text, so it cannot reopen the input-echoing problem the no-argument rule exists to prevent. 25 new tests in `tests/test_rate_limit.py` (window/gate arithmetic, per-IP isolation, a 429 with `Retry-After`, and a 503 proven to fire before the worker thread starts) plus 3 updated in `tests/test_errors.py` for the new constructor shape. `uv run pytest` → 1456 passed; `mypy`/`ruff check` clean.

- **2026-09-01** — **The frontend's API client landed, and the two halves of this project have now met**: `frontend/src/api/client.ts`, a landing page + URL input + loading state in `App.tsx`, and a dev-server proxy in `vite.config.ts` (Day 2's next-highest-value bullet, per TODO.md). No ADR — nothing here decided architecture; ADR-023 already put `/api/analyze` in place.

  - **`analyzeRepository()` does the whole round trip**: builds the snake_case `AnalyzeRequest`, `fetch`es `/api/analyze`, and on a non-2xx response tries `ApiErrorSchema` before falling back to a bare HTTP-status message — a malformed error body (e.g. a proxy's own HTML 502 page) must not throw a second, more confusing error. On success, `AnalyzeResponseSchema.safeParse` runs before anything else touches the body; a mismatch throws with the zod error message attached rather than the raw `ZodError`, since the message is the whole point for a schema whose entire pre-existing test was a fixture written by the same hand.
  - **Verified against a live backend, not just against the type checker.** `uvicorn app.main:app` plus three real `POST /api/analyze` calls (`sindresorhus/p-limit` was analyzed end to end through the dev-server proxy) confirmed `AnalyzeResponseSchema` accepts the real response verbatim, and a deliberately-wrong URL confirmed `ApiRequestError` carries the real `INVALID_REPOSITORY_URL` code, message, and request id off a live `ApiErrorSchema` body. Neither schema needed a single field changed.
  - **The proxy exists because the backend's CORS gap is real, not theoretical.** A cross-origin `POST` with a JSON body preflights, and `app/api/` sets no `Access-Control-Allow-Origin` (Day 3). Without the proxy, "wire the frontend to real data" would type-check and build but fail silently in a browser. See Broken / Known Issues.
  - **The store was left alone.** `graphStore.ts` already only held the validated response and a selection, with no derived fields — the fetch's loading/error state lives in `App.tsx`'s own `useState`, matching TODO.md's framing of this as a change to `App.tsx` rather than a reason to widen the store's stated-minimal contract.
  - **`graph/fixture.ts` was left in the tree, unreferenced by `App.tsx`.** It still parses through `AnalyzeResponseSchema` at module load, so it stays useful as a schema-regression probe (and its `<script>` rendering-guardrail probe) independent of network access; nothing else in `src/` imports it now.
- **2026-09-01** — **Component-diagram panel implemented**: `frontend/src/ui/ComponentDiagram.tsx`, `mermaid` 11.17.2 added to `frontend/package.json`, and a rewritten `componentDiagram` in `frontend/src/graph/fixture.ts`. Decisions recorded in **ADR-025** ("The component diagram is adopted from an inert document behind an element allowlist, not injected as HTML"); the SECURITY.md row it answers is "Repository comment rendered as HTML (XSS)". `npx tsc --noEmit` green. **No tests** — there are still none in `frontend/`, which is the honest headline of this entry. *(Superseded later the same day by the entry below: 35 tests landed, and they corrected this change's claim about which layer refuses a `click … href` directive.)*

  Things worth carrying forward:
  - **This is the render, not the layout decision, and the split is deliberate.** Nothing imports the panel; `App.tsx` is unchanged and the running app still shows only the canvas and the inspector. Wiring it in is a separate, cheaper decision about where a second panel goes, and keeping the two apart meant the security argument could be settled without also settling a layout.
  - **`mermaid` is the largest dependency in the project and that is a supply-chain fact, not a footnote.** 11.17.2, verified on npm before it was written into the manifest (`npm view mermaid version` → 11.17.2, also `latest`). It pulls **159 transitive packages**, including **its own copy of `cytoscape`**, so the tree now holds two cytoscape installs. `npm audit` reports 0 vulnerabilities. SECURITY.md's "Supply chain" section says the dependency count is "kept deliberately small"; it is smaller than it now is.
  - **Four config values are security controls rather than styling.** `securityLevel: 'strict'` is set explicitly even though it is the default, because it gates mermaid's whole-SVG DOMPurify pass and a default is not a control. `htmlLabels: false` is set at the **top level**, not as `flowchart.htmlLabels`, which is deprecated at 11.17.2 and logs a warning. `suppressErrorRendering: true`, because mermaid otherwise draws its own error diagram into the document. `startOnLoad: false`. (`theme`, `fontFamily`, and `flowchart.useMaxWidth` are the styling.)
  - **`mermaid.render()` returns a string, so the safety cannot come from the injection call — it comes from what is injected.** There is no `dangerouslySetInnerHTML` and no `innerHTML` anywhere in `src/`, and neither was rejected on principle: `innerHTML` neutralizes `<script>` but not `onload=`, and `DOMParser` + `importNode` is not automatically safer either, since an SVG `<script>` created through the DOM *does* execute on insertion. So: parse as `image/svg+xml` into an **inert** document (no browsing context, nothing runs while we look), refuse on an XML parse error or a non-SVG root, walk the inert tree and refuse any element outside a 15-element allowlist or any `on*` / `href` / `*:href` / `base` attribute, and only then `document.importNode`. **Refuse, not scrub** — the failure is a blank panel with a message, which is loud.
  - **The allowlist is the observed output, not a guess.** Mermaid 11.17.2 emits exactly 15 element types for a `flowchart LR` with html labels off, identical for the golden fixture and for a diagram of nothing but hostile labels. `foreignObject`, `script`, `a`, `use`, `image` and `animate` are absent on purpose; the first three are reachable through mermaid config or syntax we do not use, and refusing them is what makes this a boundary rather than a convention.
  - **The XML parse turned out to be a structural gate on `htmlLabels: false`.** Mermaid's html-label output is **not well-formed XML** — a foreignObject carrying `<img src="x">` fails with "Opening and ending tag mismatch: img". So flipping that config back on blanks the panel and says so, rather than quietly growing an HTML sink. Fail-closed on exactly the configuration mistake that matters, and it was found by trying it rather than by reasoning about it.
  - **What mermaid does is DOMPurify, not entity-escaping, and getting that backwards would misdescribe the whole panel.** `sanitizeMore` — the half that escapes `<` → `&lt;` — is **gated on html labels being ON**, so with them off it is a no-op. What runs unconditionally is the outer `DOMPurify.sanitize(text, {FORBID_TAGS:['style']})`. Read from the installed package and then confirmed in the browser: `<script>…</script>` labels disappear entirely, `<img src=x onerror=…>` survives only as the literal characters `<img src="x">` inside a `<text>` node, `<svg onload=…>` as literal `<svg> </svg>`, `</text><script>…</script>` disappears, `a < b > c & d` stays literal text. Nothing executed in any case, and the emitted SVG carried no `<script`, no `on*=`, and no `foreignObject`.
  - **All four refusal paths were driven, and each fired at the layer it was written for.** `click c0 "javascript:…"` makes mermaid emit an `<a>` (its own sanitize-url having already dropped the href) and the *element allowlist* refuses it, so no anchor entered the live DOM; `click c0 href "https://…"` emits `xlink:href` with the prefix undeclared, so the *XML parse* refuses it first; malformed Mermaid and an unknown diagram type both surface as a generic "the diagram could not be rendered", with the exception deliberately unshown because mermaid's parse errors quote the offending source line and that line is repository text. Same rule `app/errors.py` applies to the backend's own bodies.
  - **The fixture's `componentDiagram` was wrong in a way that mattered, and it is fixed.** It read `graph LR` with `src["src"]` / `api["src/api"]` — repository-derived strings as Mermaid **node identifiers**, the exact thing ADR-024 exists to make impossible. It now mirrors what the generator emits, derived from the fixture's own numbers rather than invented: one container (`c0`, six files, since all six files sit under `src`), two routes (`r0`/`r1`, from the two `serviceMap` entries), `ext` at `stats.externalImports` = 5, and no container-to-container arrow because every import edge in the fixture is internal to `src` and collapses to a self-pair.
  - **A markup probe is planted in the diagram too**, in a `c1` label: `probe <script>alert(1)</script> <img src=x onerror=alert(1)>`. It is the diagram-shaped counterpart of the `<script>` already sitting in `utils.ts`'s `description`, it is a deliberate deviation from "the fixture is derivable from the graph", and it **cannot occur in a real response** — `component_diagram._label` strips non-printables and removes twelve characters from every label: angle brackets, ampersand, double quote, hash, percent, backslash, backtick, both braces, the pipe, and the semicolon. The visible `<img src="x">` remnant is the double-encoding `component_diagram.py`'s docstring predicts: a fixture artifact, not a defect.
  - **The residual is recorded, not closed.** `<style>` is on the element allowlist because mermaid scopes its generated CSS with `#id` selectors and the panel is unreadable without it. CSS cannot execute script, so the residual is exfiltration via `url()` in a crafted stylesheet — which needs a response our own backend does not generate (repository text cannot reach a `classDef` under ADR-024), and which the planned CSP `default-src 'self'` closes.
  - **The frontend caveat is retired for this panel and for nothing else.** SECURITY.md and TODO.md both said no claim about the frontend rested on an observed render; this panel's do, in Chrome, on 2026-09-01. But it was driven from `fixture.ts` and **never from a server response** — the API client landed the same day (entry above) and `AnalyzeResponseSchema` has now validated real server output, but not through *this* panel, which nothing imports. The verification was manual and interactive, and **there are still no frontend tests**. *(Corrected 2026-09-01, later the same day: the tests landed — 35 of them, vitest + jsdom — so the verification is no longer unrepeatable, though it is still not a browser. They also corrected this entry's "each fired at its intended layer" claim.)* *(Corrected on merge, 2026-09-01: this bullet also said "there is still no API client" and "the service-map panel does not exist". Both landed the same day in parallel worktrees. `ServiceEndpoint.summary` now has a sink of its own in `src/ui/ServiceMap.tsx` — the diagram embeds route summaries inside labels, which is not the same as rendering the field — though that panel is likewise unimported and its DOM has never been inspected.)*

- **2026-09-01** — **`.mts` and `.cts` are analyzed**: the analyzed extension set went from six to eight, closing the gap TODO.md had carried since Day 1. Two lines in `pipeline._BY_EXTENSION`, two entries in `resolver.EXTENSIONS`, two in `resolver._TS_ESM_REWRITES`, and 13 tests (1336 → 1349). No ADR — this is the *implementation* of an enumerated list, not a change to how the list is decided.

  Three things worth knowing:
  - **`.mts`/`.cts` take the TypeScript grammar, not TSX**, and the reason is the one already pinned by test for `.ts`: TypeScript reads JSX only in a file named `.tsx`, so `<T>expr` in an `.mts` file is a type assertion, and handing it to TSX would fold the rest of the file into an ERROR node and silently drop the imports after it. `.mjs`/`.cjs` stay on TSX because they are JavaScript.
  - **The `.mjs` → `.mts` / `.cjs` → `.cts` rewrites map exactly one extension each**, and deliberately do *not* fall back to `.ts`. The extension in the specifier names the module kind the compiler will *emit*, and only an `.mts` source emits an `.mjs`. `test_mjs_does_not_rewrite_to_ts` predates this change and still passes — the non-rewrite it pins is a separate rule from the rewrite that was added.
  - **`.cjs` stays last in `EXTENSIONS`, and that is load-bearing for a test rather than cosmetic.** `test_a_file_beats_a_directory_index` distinguishes the file-then-directory ordering by pitting the *last* file candidate against the *first* directory one, so the new extensions went in after `.tsx` — with the TypeScript family grouped first, which is also tsc's preference — instead of being appended.

- **2026-09-01** — **The HTTP routing layer implemented**: `app/api/{routes,middleware,app}.py` and `app/main.py`, plus `GraphLimits` and the cap logic in `app/analysis/graph_builder.py`, and 35 tests (1336 → 1371). ARCHITECTURE.md's "API Boundaries" section moved from `Planned` to `Implemented` and its `app/api/` row to `In progress`; the "Graph construction" section's `MAX_NODES`/`MAX_EDGES` paragraph was **inverted** rather than flipped. **Five SECURITY.md rows moved** — "Unbounded graph", "Oversized request body", "Event loop starvation", and both "Information disclosure via errors" rows — and four more were updated without moving. Three items came off SECURITY.md's "still required" list.

  Decisions worth carrying forward, all recorded in ADR-023:
  - **The cap belongs wherever the numbers are still being produced.** ADR-018 offered the router two ways to enforce `MAX_NODES`/`MAX_EDGES`; the first — re-derive the stats after truncating — is not a hand-off but a re-implementation, because a slice falsifies `stats.dependencies == len(edges)`, every per-node `imports`/`importedBy`, `stats.files`, `stats.directories`, both import totals, and every directory aggregate, and can leave an edge naming a node that is gone. So `build_graph` takes a `GraphLimits` and builds a smaller graph. Same trade ADR-019 made when it capped imports upstream rather than downstream, hit for the third time.
  - **Component ordering paid off a second time.** A path's parent is a prefix of it in `parts` order, so a parent always sorts first and **any prefix of the sorted node list is closed under `parent`** — dropping the tail cannot orphan a survivor, with no repair pass. ADR-018 chose that ordering so the frontend could build a tree in one forward pass; it turns out to be what makes the cap expressible at all. A "smarter" policy (drop the least-connected leaves) would need its own closure argument.
  - **FastAPI swallows a typed error raised from `receive()`.** `fastapi.routing.get_request_handler` wraps the body read in `except Exception: raise HTTPException(400, "There was an error parsing the body")`, so the tidy design for the body cap — raise `PayloadTooLargeError` and let the one `AppError` handler shape it — reports a **400 "malformed JSON"** instead of a 413. Measured against fastapi 0.141.1. The middleware writes its own response, which is also the stronger property: an oversized body never reaches application code.
  - **The `asyncio.wait_for` net was declined, not forgotten.** Every planning document called for one. It cannot kill a thread, so it returns a 504 while the work continues, and a client retrying on that 504 *adds* live threads — a timeout that makes an overloaded server worse. The bound is the cooperative one that already exists; shedding load is `MAX_CONCURRENT_ANALYSES`' job.
  - **Four handlers, because three leave a hole.** `AppError` and `RequestValidationError` were the brief; Starlette's own `HTTPException` was not, and without it a 404 on an unknown path and a 405 on a wrong method are the two most reachable responses in the service and the only two answering `{"detail": …}` instead of the contracted body.
  - **The `RequestValidationError` override is a control, not a default.** FastAPI's own handler returns pydantic's `detail`, which embeds the client's input verbatim. The test posts a marker in an unexpected field and asserts the marker, the key `loc`, and the field name are all absent; removing the handler fails it.
  - **`/docs` and `/redoc` are off; `/openapi.json` is on.** Both rendered pages load JavaScript from a public CDN, which would make a third-party script the only remote resource this backend serves — for a convenience the frontend gets from the schema.
  - **`configure_logging()` moved out of the factory.** It clears the root logger's handlers, which would tear `caplog` out from under any test that built an app. It lives in `app/main.py`, which only a real run imports.
  - **`starlette.testclient` is not used.** Its httpx integration is deprecated in this Starlette and emits a warning that `filterwarnings = ["error"]` turns into a failure. `httpx.ASGITransport` drives the same app in process, opens no socket, and needs no new dependency.
  - **19 controls were mutation-tested one at a time; all 19 are caught**, but two needed work. One apparent survivor was a **no-op mutation** — `_nodes` is only ever handed the surviving files, so there was no line to delete; rewritten as passing the full mapping it fails four tests. The other was a **real gap**: deleting the declared-`Content-Length` check left the suite green, because the byte counter catches the same request one cap's worth of bytes later. The difference the header buys is whether the body is *read*, so a test now asserts not one chunk is pulled.
  - **The service map is deliberately not filtered when the graph is capped**, even when the file declaring a route is no longer a node. It is a separate view keyed by path, capped independently, and rendered as a list; shortening a repository's reported API surface because a *graph size* cap fired would make a deterministic list quietly wrong. A test pins it, including the dangling `endpoint.file`.

- **2026-09-01** — **Component diagram implemented**: `app/analysis/component_diagram.py` plus 46 tests (1336 → 1382) and `tests/fixtures/component_diagram_golden.mmd`, the project's first golden file. ARCHITECTURE.md gained a "Component diagram" section at `Implemented`; **two SECURITY.md rows moved** in the "Repository-authored text in responses" section, whose title gained a third sink, and `MAX_COMPONENT_DIAGRAM_CHARS` stopped being the last limit in `Settings` that bounded nothing.

  Decisions worth carrying forward, all recorded in ADR-024:
  - **The external system is one unnamed box with a count, and that follows from ADR-005 rather than from laziness.** A bare specifier is a count on the importing node and never a node of its own, so by the time the graph is finished the package *names* are gone — `resolver.ResolvedImport.specifier` is the last place one exists. Naming them means taking the resolver's output as a second input and putting package specifiers into a response body, which is an ADR-013-sized decision about a *sink*, not a detail of a drawing. `graph_builder.py`'s docstring calls package-name extraction "the component diagram's job later"; the seam is one parameter wide when someone decides to open it.
  - **Synthetic node ids are the control; the sanitizer is the second line.** Directory names, route paths and comments are attacker-controlled and now land inside a *format*. Sanitizing is a denylist, and a denylist is a claim about a grammar owned by a renderer we do not control at a version we do not pin. `c0` / `r0` / `ext` remove the position from which repository text could become syntax at all — CLAUDE.md's "eliminate the class" applied to a text format — and the test asserts that structurally, by checking no marker string appears outside a quoted label, rather than by enumerating characters.
  - **Containers come from *file* paths, not from directory nodes.** Reading `depth == 1` directory nodes is one line shorter and silently wrong on one real input: ADR-018 resolves `components.ts` beside `components.ts/x.ts` in the file's favour and creates no directory node, so `x.ts`'s container would vanish. First-path-component needs no node to exist.
  - **The character cap is applied while writing, because truncated Mermaid is not Mermaid.** ADR-020's "cap while cleaning" restated for output that has structure. A block's closing `end` is reserved before its header is written, a block the budget emptied is rewound, and an arrow is written only when both endpoints were emitted — Mermaid does not reject a dangling id, it invents a box labelled `r17`, which is our truncation presented as a fact about the repository.
  - **Containers are written before routes, and that is a priority rather than a layout.** A budget too small for everything should spend itself on the thing ADR-013 names first; in an `LR` flowchart it is the arrows that place a box, so the picture is unchanged.
  - **The item caps and the character cap are different tools.** 24 containers / 30 routes / 80 container edges exist for legibility — a repository with 300 top-level directories has no readable diagram at any size — and are sized so the worst legible case measures **8 685 characters against a 20 000 default**. That keeps the character limit a safety net rather than the thing shaping ordinary output; the test that pins it fails if the two ever drift together.
  - **29 controls were mutation-tested one at a time; 27 are caught and both survivors are equivalent by construction**, each annotated in the code. Three of the five initial survivors were real test gaps and are now closed: the root container's ordering (component order versus string order — ADR-018's `src-b` case arriving from the other side, catchable only with a directory named `-lib`), an emptied subgraph left unclosed rather than rewound, and a `-->|0|` arrow from a container with no external imports, which needed *two* containers to be a real test.
  - **No route summary is glued on as `(unnamed)`.** A comment of nothing but bidi overrides normalizes to an empty string, and the separator goes with it — the route is still a route, only the gloss is gone.

- **2026-09-01** — **The Day 1 golden-file test landed**: `tests/test_api_routes.py` gained `test_the_whole_response_matches_the_golden_file` and `test_the_same_commit_produces_byte_identical_json` (1431 → 1433), plus `tests/fixtures/analyze_response_golden.json`, the project's second golden file and the first over a whole `AnalyzeResponse` rather than one field. Closes Day 1's last open item (`docs/TODO.md`) — this backend day now has nothing outstanding.

  Decisions worth carrying forward:
  - **The golden file is a pretty-printed `response.json()`, not `response.text`.** `json.dumps(response.json(), indent=2, ensure_ascii=False) + "\n"` is what is compared, so the fixture is reviewable on disk the way `component_diagram_golden.mmd` is, rather than one unreadable line. Python preserves both dict insertion order (from `json.loads`) and pydantic's field order, so re-serializing does not risk silently reordering keys away from what the wire actually sent.
  - **Byte-identity is a second, separate test, not an assumption folded into the first.** The golden-file test only proves the response matches a fixture captured once; it says nothing about whether a second call would match the first. `test_the_same_commit_produces_byte_identical_json` calls `/api/analyze` twice against the same mocked repository and diffs the raw response text, which is the actual shape of ADR-013's claim ("the same commit must produce byte-identical JSON") and would catch what a single capture cannot: an unstable ordering, an uncontrolled clock, or a leaked id.
  - **The fixture reuses `REALISTIC` and `serve()`**, already in the file for `test_analyzes_a_repository_over_http`, rather than introducing a second repository shape. One fewer fixture to keep synchronized with the golden file's content.

- **2026-09-01** — **`frontend/` scaffolded** (Day 2, bullets 1–3): Vite 8 + React 19 + TypeScript 5.9.3 + Tailwind v4 + Zustand 5 + zod 4 + Cytoscape.js 3.34, the zod wire schema, and a Cytoscape canvas rendering a hand-written fixture with directory hierarchy as compound nodes. ARCHITECTURE.md's Frontend section moved from `Planned` to `In progress`, with per-subsection markers, and its "API → Browser" security-boundary row was updated. **No ADR was added** — ADR-022 already decided the rendering library, and nothing here decided anything it did not.

  Things worth carrying forward:
  - **The zod schema is a transcription, not an interpretation, and the mapping is mechanical**: `extra="forbid"` → `strictObject`, a pydantic field with a default → `.nullish().default(...)`, a field without one → required even when nullable (`GraphNode.parent` is the deliberate case), `Field(ge=0)` → `.int().min(0)`, each `AfterValidator(_within_*_limit)` → `.max(LIMITS.*)`. Written that way so the next person can diff it against the two Python files rather than reason about it.
  - **It deliberately does *not* assert `stats.dependencies === edges.length`.** ARCHITECTURE.md is explicit that the models cannot enforce that either — it is the graph builder's job — and a client that enforces a stricter contract than the server publishes turns a backend counting bug into a blank screen instead of a slightly wrong number.
  - **`ADR-006`'s `parent` field is the entire compound-node transform.** ADR-022 predicted this ("Cytoscape's compound nodes consume a `parent`-shaped hierarchy directly") and it held exactly: `elements.ts` renames one field and does nothing else structural. The graph-shape decisions made months before the rendering library was chosen survived the change of library untouched, which is the payoff for keeping them separate.
  - **Two defensive drops were added at a boundary that "cannot" fail.** An unknown `parent` or a dangling edge makes `cy.add()` throw, which takes down the whole canvas rather than losing one element. ADR-018 makes both impossible; the drop costs four lines and turns a total failure into a counted one. Only the *count* is logged — a path is repository content.
  - **The invisible-canvas trap is a React/Cytoscape interaction, not a Cytoscape bug**, and it is recorded above under Broken / Known Issues because the failure mode is silence: correct positions, correct element count, zero pixels.
  - **`@types/cytoscape` was installed and then removed**: npm deprecates it as a stub because Cytoscape ships its own types. Installing it first and reading the warning was cheaper than assuming either way, and this is the "verify a version exists before writing it into a manifest" rule extended to whether the package should be there at all.
  - **The rendering guardrail is structural, and a probe is planted for it.** A fixture description contains `<script>`, and `Inspector.tsx` interpolates it as a React child; `grep` finds no `dangerouslySetInnerHTML` or `innerHTML` anywhere in `src/`, and Cytoscape paints labels to a canvas, which has no markup parser. **That is a claim about the code path, not an observed render** — CLAUDE.md's rule against asserting a control you have not seen cuts both ways, and nobody has loaded the page and inspected the DOM. *(**Superseded 2026-09-01** by the component-diagram entry above: both sinks have since been inspected in a live DOM. The replacement caveat is narrower — the inspection was manual and there are still no tests.)* *(**And narrower again**, later the same day: `no-markup-sinks.test.ts` now asserts that grep as a test, so this bullet's structural claim is checked rather than restated.)*
  - **The frontend has no test suite**, so every claim above rests on reading the code rather than exercising it. That is the honest status and it is why the first real `/api/analyze` response should be treated as the schema's first test. *(**Closed 2026-09-01**, later the same day: 35 tests, vitest + jsdom. The schema half of the suggestion is still open — no frontend test yet reads `backend/tests/fixtures/analyze_response_golden.json`.)*

- **2026-08-31** — **Route detection implemented**: `app/analysis/routes.py` plus 59 tests and 10 more in the pipeline (1267 → 1336), a field on `SourceFile`, a `service_map` property, and a two-way split of `parser.py`. ARCHITECTURE.md gained a "Route detection" section at `Implemented`; **two SECURITY.md rows moved** — `MAX_ENDPOINT_SUMMARY_CHARS` and `MAX_SERVICE_ENDPOINTS` had never bound anything before — and two stale symbol names were corrected while in that file (`parser._collect` → `_parse`, `parser._specifier` → `string_literal_text`).

  Decisions worth carrying forward, all recorded in ADR-021:
  - **The tree became a seam, and the reason is guard duplication rather than cost.** `parse_source` now owns all five guards between untrusted bytes and tree-sitter; `extract_imports` is just the query. The alternative was a second module that re-parses — `descriptions.py`'s shape — and its cost was addressable with a byte prefilter, but a second copy of `_is_pathological` was not. That guard is the only thing between the query engine and an eleven-minute traversal (ADR-010), and a security control with two implementations has two chances to be wrong.
  - **ADR-020's objection to widening the parser does not transfer, and the asymmetry *is* the design.** Widening for descriptions would have coupled a header comment to a successful parse, so a binary file would have silently lost it. For routes that coupling costs nothing: a file with no tree has no locatable route no matter who looks. Same question both times — does this fact exist independently of a successful parse? — opposite answers.
  - **A wrong endpoint is worse than a wrong edge, so the verb set is not the whole filter.** `map.get('key')` passes a verb test. The first argument must be a string literal starting with `/` and there must be a handler after it — the second condition is also what separates a registration from Express's own `app.get('trust proxy')` settings getter. A spurious edge is one line in a graph of thousands; a spurious endpoint is one row in a service map of six, where nothing distinguishes it from a real one.
  - **Laziness is a bound, not a style.** `_routes` yields rather than returning a list, because the pipeline's `MAX_SERVICE_ENDPOINTS` otherwise stops collection *after* every endpoint in the file has been built. Measured on the densest legal input — a full `MAX_PARSE_BYTES` of `app.get('/a',h);` — that is **61 680 records built to keep 200, 1.11 s versus 0.31 s**. A test counts model constructions, since the difference is invisible in the return value.
  - **A tree-sitter `//` run is one node per line.** `prev_named_sibling` returns only the last line, so a two-line comment above a handler silently lost its first line. Found by a failing test rather than by reading the grammar, and it is the one place where locating from the tree is *harder* than locating from bytes — `descriptions._line_run` gets it free. The reassembled run is never glued to a block comment above it, because `normalize_comment` dispatches on the first two characters.
  - **`_endpoint` catches `ValidationError`, and mutation testing is why.** Anything raised there escapes into `detect_routes`' catch-all, which abandons the *whole file* — so one malformed record silently deleted every other route beside it, indistinguishable from a file with no routes. Two separate mutations survived by hiding behind exactly that. One bad record must cost one record.
  - **The `except AnalysisTimeoutError: raise` was dead code and is gone.** The deadline check sits outside the try, so a timeout is structurally unswallowable rather than defended against — ADR-009's pattern applied to control flow. Mutation testing found the re-raise unreachable.
  - **`tree.language` returns the identical `Language` object**, so the `lru_cache`d query compilation still hits. Checked *before* committing to the design: a miss per file would cost ~8.8 ms × 3000 ≈ 26 s of a 60 s budget.
  - **The third traversal is cheap on real input** — across 3000 ordinary files: parse 1.45 s, import query 0.94 s, route query **0.48 s**, 17% of the total.
  - **`MAX_SERVICE_ENDPOINTS` does not set `truncated`.** The other two caps abandon the download; this one leaves the graph complete. `routes_truncated` is separate for the same reason `imports_truncated` is.
  - **30 controls were mutation-tested one at a time; 25 are caught and all 5 survivors are equivalent by construction**, each annotated in the code. Notably one of them is the `comment` node-type test: with it removed, `normalize_comment` still refuses the statement text it is then handed — the **first evidence that ADR-020's safety net actually holds**. It is kept anyway, because relying on a downstream refusal to keep source code out of a response is a worse contract than not sending it.
  - **The secret-filter ordering test got strictly stronger for free.** It spied on `extract_imports`; it now spies on `parse_source`, so it asserts filtered bytes never reach the *parser* and therefore no reader of the tree. Under the old arrangement, adding a second reader with its own parse would have left it green.

- **2026-08-31** — **Description extractor implemented**: `app/analysis/descriptions.py` plus 63 tests (1204 → 1267), a field on `SourceFile`, and one line in the graph builder. ARCHITECTURE.md's "Description extraction" section moved from `Planned` to `Implemented` — **and had to be corrected, not just flipped**: it said the extractor runs "over the tree `extract_imports` already built", and it does not. **Three SECURITY.md rows moved** in the "Repository-authored text in responses" section, which had a call site for the first time.

  Decisions worth carrying forward, all recorded in ADR-020:
  - **No tree, and the reason is correctness before cost.** The three options were widening `extract_imports`, a second module that re-parses, and a prefix scan. The scan wins because **at byte 0 there is no lexical context to get wrong**: every ambiguity in JS tokenization — regex versus division, `//` inside a string, `/*` inside a comment — is a question about what *preceded* the position, and nothing precedes the first byte of a file. The scanner is not an approximation of the parser there; it is the same answer.
  - **The cost difference is real but it is the second argument, not the first.** A second parse is ~5.7 s for 3000 files and ~3.1 s for the worst single hostile file. Measured for the scan across the whole `MAX_SOURCE_FILES` cap: 0.003 s when files have no header comment (the common case — the first non-whitespace byte is not a `/`), 0.031 s for ordinary JSDoc, 0.214 s for a comment longer than the window, and **0.706 s for the worst adversarial input found** (a 4 KiB window of NUL bytes).
  - **The module splits into a locator and a normalizer, and that split is the whole answer to "the same extractor serves route summaries".** It does not — half of it does. `ServiceEndpoint.summary` is the comment above a handler, deep in a file, where the byte-0 argument *reverses* and only the tree can locate it unambiguously. So route detection will find that comment as a sibling node and call `normalize_comment(node.text)`. Locating is per-caller; normalizing is shared. Building the second caller now was explicitly out of scope; deciding its seam was not.
  - **Widening `extract_imports` would have coupled a description to a successful parse.** `_collect` returns early for oversized, binary, and pathologically malformed files, so exactly the files that produce no imports would also have silently lost their header comment. With no tree in the path, a binary file that opens with `/** Generated blob. */` keeps its description and its node. Pinned by test.
  - **`str.isprintable()` does the control-character strip, and it covers Trojan Source for free.** It is False for C0/C1 controls, surrogates, unassigned code points, and the `Cf` format characters — which include U+202E RIGHT-TO-LEFT OVERRIDE, a display-spoofing primitive aimed at exactly the sink a description lands in. Getting that by luck rather than by design is worth a test, so it has one.
  - **Undecodable bytes are replaced, not refused — the opposite of `parser._specifier`, deliberately.** A specifier is compared to an archive path byte-for-byte, so U+FFFD there invents an edge that can never resolve. A description is only displayed. And strict decoding gets one case actively wrong: the scan window is a fixed byte count and can land mid-character in a *valid* UTF-8 file, discarding that file's whole description for a reason having nothing to do with the file.
  - **The cap is applied while cleaning, not after.** Cleaning removes characters, so no raw length reliably yields 500 clean ones; the alternatives were cleaning the whole comment first (unbounded work on text already known to be cut) or guessing a multiplier that is wrong for some encoding, silently. Counting output characters is exact and makes the loop bounded by the limit.
  - **`normalize_comment` refuses input that is not comment syntax.** It is not a general text sanitizer. If route detection ever hands it the wrong node, the failure should be an absent summary and not a line of source code in a response body.
  - **"A secret file producing a description" is closed; "a secret *in* a description" is not, and the row's title was hiding that.** `is_secret_path` runs first in the loop and the extractor runs last, so a `.env` produces no record at all — verified against the call site, not assumed. But it is a rule about *paths*: `src/config.ts` whose header comment contains an API key is quoted verbatim, because the filter deliberately has no heuristics over file bodies. Recorded as a residual in SECURITY.md rather than left implied by the row's title.
  - **23 controls were mutation-tested one at a time; all 23 are caught, and there is nothing to annotate.** Notable because it is the opposite of the last three modules, which each shipped with an annotated survivor. Three mutations initially survived and **all three were genuine test gaps rather than equivalent mutations**: `normalize_comment`'s decode had no direct coverage, `* ** bold **` cannot distinguish "drop one star" from "drop the run of stars" (a space follows either way — `** Double.` discriminates), and every `//` fixture was written `// One.` with the conventional space, which hides a dropped line break because the space separates the words anyway. `//One.` was the input that caught it.
  - **The scan window is a real bound with a real cost**, and every other test in the file passes with the slice deleted. A description beginning after 4 KiB of padding is not found; that now has its own test, because a work bound nothing observes is indistinguishable from a bound nobody wrote.

- **2026-08-31** — **`MAX_IMPORTS` implemented**: the pipeline now caps the total import count, closing the last structural DoS before the router (ADR-019). `app/config.py` gains one limit, `app/analysis/pipeline.py` gains the counter and `RepositoryAnalysis.imports_truncated`, plus 9 tests (1195 → 1204). SECURITY.md's "Post-parse analysis runs outside the deadline" row moved from **Missing** to **Implemented for the import half** — the graph-size half (`MAX_NODES`/`MAX_EDGES`) is still open and still the router's.

  Measured on the fixture from the original report — 3000 files × 334 unresolvable relative imports, 21.7 MiB extracted:

  | | imports carried | parse | resolve | total | flags |
  |---|---|---|---|---|---|
  | Before | 1 002 000 | 5.7 s | **76.1 s** | 81.8 s | none |
  | After | 100 000 | 0.6 s | **7.7 s** | 8.2 s | `truncated`, `imports_truncated` |

  Decisions worth carrying forward, all recorded in ADR-019:
  - **A count, not a clock, and the resolver still takes no `Deadline`.** Threading one through was the obvious fix and is the worse one: resolution is not incremental work that can stop halfway and still mean something, so a time-bounded run yields a graph whose missing edges are indistinguishable from genuinely unresolvable imports, and which differs between two runs of the same commit. CLAUDE.md requires the response to be a pure function of the commit; a count keeps that, a clock does not.
  - **It has to be capped upstream, because downstream cannot.** The resolver would have to resolve the imports in order to discard them, and the graph builder computes `stats.dependencies == len(edges)` and the per-node `imports`/`importedBy` counters from what it is handed — so slicing its output falsifies all three. This is ADR-018's "the hand-off is not free" bullet, hit for the second time and from the other side.
  - **Counted per import, not per file.** A single `MAX_PARSE_BYTES` file can hold ~87 000 twelve-byte `import"./a"` statements, so a per-file check would overshoot by most of the cap again. The consequence is that the stop can land *inside* a file.
  - **The partially-read file is kept, not dropped.** Its bytes and line count are true, other files may legitimately import it, and dropping it would delete a node to hide a short list that `imports_truncated` already reports. Same call as ADR-018's file/directory collision and the pipeline's "a file the parser gave up on is still a node".
  - **`imports_truncated` is a field, not a `skipped` key** — the task brief offered both. `skipped` counts files that produced **no** node and is what `skipped_files` sums; the capped file produced one. Folding it in would inflate a file count with a non-file, which is exactly what `_NON_FILE_SKIPS` already exists to prevent for directory entries, and would break `len(files) + skipped_files` as a description of the archive.
  - **`truncated` no longer means `MAX_SOURCE_FILES`**, it means *a* cap fired. The two are distinguishable because their consequences differ: the file cap drops whole files off the end of archive order, the import cap can also leave the last file with a partial import list. A test pins the distinction.
  - **`import_count` is a derived property, not a stored field**, so it cannot drift from `files`; it is by construction the length of what `resolve_imports` returns.
  - **Six mutations were tested one at a time; all six are caught** — the check deleted, `>` for `>=`, the counter reset per file, the inner `break` without the outer one, `imports_truncated` set without `truncated`, and the partial file dropped instead of kept. No survivors, so nothing needed a redundancy annotation.
  - **Two stale symbol names in SECURITY.md were corrected while in that table**: `pipeline._consume` → `_analyze` and `pipeline.analyze` → `analyze_repository`. Neither function ever existed under the documented name — leftovers of the PR #2/#3 merge.

- **2026-08-31** — **Graph builder implemented**: `app/analysis/graph_builder.py` plus 36 tests (1159 → 1195). ARCHITECTURE.md gained a "Graph construction" section at `Implemented` and its "Graph model" section stopped saying the builder does not exist. **No SECURITY.md row moved**, and that is correct: the builder lands no new control. Two existing rows were *tightened* rather than flipped — the ADR-003 no-disk claim now covers graph construction, and the log-hygiene row's graph-construction half is closed structurally — and the "node/edge limit enforcement" gap was sharpened, because the module that produces unbounded nodes and edges now exists.

  Decisions worth carrying forward, all recorded in ADR-018:
  - **The repository root is a node, and it is named for the repository.** `PurePosixPath(".")` is not invented — it is where `.parents` terminates for every path in the analysis, its `str()` is `"."` so it satisfies the contract's `min_length=1` without a special case, and its `parts` is `()` so it sorts first for free. Its `.name` is `""`, which the contract forbids and which would be a useless label anyway. A forest of parentless top-level nodes was the alternative; it contradicts ADR-006's singular "the root" and loses `root.fileCount == stats.files`.
  - **Ordering is by path *components*, not by path string, and the difference is load-bearing.** `sorted` on strings places `src/a.ts` *after* `src-b`, because `-` is 0x2d and `/` is 0x2f — a directory gets separated from its own children by an unrelated sibling. Component order also puts the root first. The result is that **a parent always precedes its children**, so a consumer builds the tree in one forward pass. Both cases are tests; the string-sort mutation fails one of them. Anyone "simplifying" this key to `str` will pass 33 of 36 tests.
  - **`node.imports` is the outbound edge count, not the raw import count.** Otherwise `imports` and `importedBy` would count different things while sitting side by side in the inspector. Off the *finished* edge set means `sum(imports) == sum(importedBy) == len(edges) == stats.dependencies` — four numbers that agree, asserted together over a 300-file fixture rather than one at a time.
  - **`externalImports` counts statements, not distinct packages.** Dedup is specified for edges and only for edges, and the resolver deliberately does not extract a package name from a specifier, so any dedup here would be a guess. Two imports from `react` count two.
  - **A tarball can carry `components.ts` *and* `components.ts/x.ts`, and both survive every existing filter.** They want the same node id. Resolved as **observed beats inferred**: the file node is kept and no directory node is created, so ids stay unique, no edge dangles, no count lies, and the child's `parent` still names a node that exists. It renders as a file with children — strange and true. Raising instead would let one odd archive fail an entire analysis, which is the wrong trade for input we did not write; the same reasoning that makes an unresolvable import a count rather than an error.
  - **A repeated path is one node built from the first record**, for the same reason.
  - **Two preconditions raise `ValueError`, with no path in the message.** A resolved import whose `source` or `target` is not in the analysis is a programming error — unreachable when the resolver is the producer, reachable the moment someone pairs mismatched inputs — and a dangling edge must not be the outcome. `path_safety.safe_relative_path`'s reasoning, applied again: a low-level utility raising a plain `ValueError`, not an `AppError`, with a fixed literal message so no path is echoed. A test asserts the message quotes nothing.
  - **No logger and no filesystem, both pinned structurally rather than claimed.** One test builds a graph with `os.stat`/`lstat`/`listdir`/`scandir`/`open`/`access`/`readlink`/`mkdir` torn out; one asserts a full build emits no log record at any level. Both fail when the corresponding mutation is applied. This is the resolver's argument re-used, and it is what let two SECURITY.md rows be tightened.
  - **It is deliberately uncapped, and that is now the sharpest open control in the project.** See Broken / Known Issues — the hand-off to the router is not free, because capping after the fact falsifies the stats the builder computes. *(**Closed 2026-09-01**, ADR-023: the router asks the builder for a smaller graph rather than slicing its output. Uncapped is still the default.)*
  - **22 controls were mutation-tested one at a time; 21 are caught.** The survivor is annotated in the code: the `target is None` test in `_edges` is redundant *by construction* — `ResolvedImport.__post_init__` already enforces that a target is set exactly when the resolution is `RESOLVED` — and is kept because it is also what narrows the type, which a `cast` would do less honestly. Three further "survivors" turned out to be *equivalent* mutations rather than gaps (`"" or name`, and `len(path.parents) == len(path.parts)` for every relative path); rewritten as real mutations, all three are caught. Worth knowing when mutation-testing this module: `PurePosixPath` makes several plausible-looking mutations no-ops.

- **2026-08-31** — **MVP module resolver implemented**: `app/analysis/resolver.py` plus 74 tests (1085 → 1159). ARCHITECTURE.md's "Dependency extraction / resolution" section moved from `Planned` to `In progress`. **No SECURITY.md row moved, and that is correct** — the resolver lands no new control. What it does is make one *architecturally*: resolution is set membership, so there is no path check to implement and none was.

  Decisions worth carrying forward:
  - **Set membership is the whole security argument, and it replaces three checks rather than passing them.** There is no `Path.exists`, no `os.stat`, and no traversal guard, because the target set *is* the node set: a resolved target is always a node, so the graph builder cannot emit an edge with no node on the far end, and `../../../../etc/passwd` is inert because nothing here reads anything. A test tears `os.stat`/`lstat`/`scandir`/`listdir`/`open` out from under a full resolution to keep that structural rather than incidental.
  - **The TS ESM rewrite is tried before the literal `.js`, and the test proves the order rather than the map.** `./util.js` in a TypeScript project means `util.ts`; the extension in the specifier names the file the compiler will *emit*. The fixture ships `util.ts` **and** `util.js`, so flipping the order fails it — an outcome-only test against a repository containing just one of them would pass either way, and the failure mode being guarded is a graph that silently draws its edges to build output.
  - **`..` failing is not the same as `..` clamping.** `_join` returns `None` when a specifier climbs above the root instead of stopping at it. Clamping would fold `../../../../etc/passwd` into `etc/passwd` and then resolve it against a repository that happened to contain that path. Both behaviours miss the set on today's fixtures, which is exactly why it needed its own test.
  - **The no-op-segment skip looked redundant and is not.** `PurePosixPath` collapses a `.` component by itself, so deleting the skip leaves almost everything green — until a later `..` pops the `.` instead of popping a real directory and `./a/./../util` resolves one level too deep. Found by mutation, now two tests.
  - **A specifier that cannot be resolved is not automatically external.** `/abs/path`, `#subpath`, and malformed dot-forms are counted as *unresolved*, because counting them as external would report a dependency on a package that does not exist. Each of the three characters in that rule is separately mutation-tested.
  - **`target` is set exactly when the resolution is `RESOLVED`, enforced in `__post_init__`.** The alternative surfaces downstream as an edge to a node the user never imported. Same reasoning as `AppError.__init__` taking no arguments.
  - **The module has no logger at all.** docs/SECURITY.md forbids logging import specifiers, and specifiers are the one thing this module handles. A module with no logger cannot leak one — the ADR-009 pattern applied to a log line.
  - **Output is one record per import** (ADR-017), which makes `len(result) == sum(len(f.imports))` a single assertion, the same kind of checkable identity as the pipeline's `len(files) + skipped_files`.
  - **The config seam is decided and deliberately unbuilt** (ADR-017): `tsconfig` will be parsed inside the pipeline loop and carried already-narrowed on `RepositoryAnalysis`. Raw config *bytes* would break the ADR-016 invariant a test already pins, and a second harvest pass would mean a second download, because ADR-003 keeps nothing.
  - **23 controls were mutation-tested one at a time; 22 are caught.** The survivor is annotated in the code: the empty-`parts` guard in `_candidates` is redundant *by construction*, since `_join` only returns an empty list for specifiers that `_is_directory_form` already flags.
  - **It has no caller.** The graph builder is the first one. Grep for the call, not the module.

- **2026-08-31** — **The pipeline was run against real GitHub repositories for the first time.** `backend/scripts/smoke.py` added; `[tool.mypy] files` extended to cover `scripts`. No production code changed — the pipeline worked on the first real run.

  Results, on Python 3.14.7:

  | Repository | Files | Imports | Elapsed | Skipped |
  |---|---|---|---|---|
  | `sindresorhus/p-limit` | 6 (4 js / 2 ts) | 24 | 1.5 s | 1 secret, 9 unsupported |
  | `pmndrs/zustand` | 50 (16 js / 34 ts) | 163 | 2.2 s | 94 unsupported |
  | `sindresorhus/ky` | 54 (54 ts) | 186 | 2.0 s | 1 secret, 15 unsupported |

  `RepositoryNotFoundError` was confirmed against a genuinely nonexistent repository and `NoSupportedFilesError` against `octocat/Hello-World`. Every commit SHA was harvested from the archive root, which is the ADR-011 channel working on data we did not construct.

  Things worth carrying forward:
  - **It is deliberately not a pytest test.** `tests/conftest.py` blocks the socket entry points at session scope and its docstring makes that a guarantee rather than a default — a per-test `monkeypatch` undo restores the *block*, not the real socket. A `network` marker that lifted it would place an escape hatch in the one place the project promises there is none, and the hatch would then be available to every future test. A separate entry point keeps the suite hermetic by construction, which is the same "eliminate the class rather than defend against it" reasoning as ADR-009.
  - **The one secret-filtered file in two of the three repositories is `.npmrc`**, and that is correct rather than a false positive: npm writes `//registry.npmjs.org/:_authToken=…` into exactly that file. Worth recording because a filtered count of 1 on a repository with no secrets looks like a bug until you check.
  - **`.github/` is *not* caught by the `.git` directory rule.** `_EXCLUDED_DIRS` is exact set membership, not a prefix test, so `.github/workflows/main.yml` falls through to `unsupported_extension` as it should. This was the first thing checked when the skip count looked surprising; had the rule been a prefix, every composite GitHub Action's `index.js` would have silently vanished from the graph.
  - **Timing has a lot of headroom.** 54 files in 2.0 s against a 60 s budget, so the deadline is nowhere near binding on repositories of this size. That says nothing about the 3000-file case.
  - **The smoke output prints counts and extensions only** — never a specifier, never a path, never a token — because it is the kind of output that gets pasted into an issue. The `probe` that listed paths to explain the `.npmrc` count was a scratch file and is not checked in.

- **2026-08-31** — **LLM layer cut; wire contract amended (ADR-013 supersedes ADR-012).** Documentation across `CLAUDE.md`, `ARCHITECTURE.md`, `SECURITY.md`, `TODO.md` and this file, plus the minimal code change that keeps them honest: `c4` → `componentDiagram`, `MAX_C4_CHARS` → `MAX_COMPONENT_DIAGRAM_CHARS`, new `GraphNode.description` and `MAX_DESCRIPTION_CHARS`, and 3 tests (1071 → 1074). Suite, mypy strict, and ruff all green.

  Decisions worth carrying forward:
  - **This deletes a trust boundary rather than defending one.** ADR-012 had added five `Planned` SECURITY.md rows for sending repository content to an LLM. Four are now *eliminated* — no prompt, no provider, no key — not mitigated. Only one survives, and it survives because it was never about the model: descriptions are the **first repository *content* to enter a response body** (everything before was paths, counts, line numbers), so the "render as text, never as HTML" rule outlives the LLM that motivated it.
  - **The description is a quotation, not a generation.** It is the file's own leading header comment, so it can be absent but never wrong. That is the deliberate trade against an LLM's better coverage: a confidently fabricated description of a file the user wrote is the worst failure available in a demo.
  - **`/api/explain` was deletable, not portable.** It existed to fetch a file lazily at a pinned SHA because per-file LLM calls cost money. A comment extractor needs no fetch at all — the bytes are already in memory during the parse — so the endpoint, the second network path, the HMAC token, the client cache and the loading state all disappear together rather than being reimplemented.
  - **Consequently ADR-007's mechanism went back to Deferred as one unit.** `/api/explain` was its only in-scope caller. `GraphNode.sourceToken` stays in the contract and stays `None` — a field belonging to a deferred design, not a dead one.
  - **The field was renamed, not just repurposed.** A diagram derived from imports is a component sketch; C4 encodes *intent*, which no import graph carries. Leaving it called `c4` would have made the wire contract promise something the generator cannot deliver.
  - **The absent-by-default encoding was kept, with its reasoning replaced.** It used to encode "an LLM may fail independently of the graph". It now encodes something more mundane and more often true: **most files carry no header comment**, so absent is the ordinary case, not a degraded one. Same types, different justification — worth knowing, because the old comment would have led someone to treat `None` as an error path.
  - **The bound moved into `graph.py` reading `Settings`**, matching `api.py`'s discipline rather than hardcoding 500. **Three mutations were tested and all three are caught**: deleting the length check, hardcoding the limit past `get_settings()`, and dropping `min_length=1`.
  - **A golden-file test over a whole `AnalyzeResponse` is now possible** and is on the Day 1 list. It was not possible with a model in the path.

- **2026-08-31** — **Ingestion+parse pipeline implemented**: `app/analysis/pipeline.py`, plus the commit-SHA harvest in `app/fetch/archive.py`, and 58 tests (1013 → 1071). **This is the first code path in the project that joins two modules to each other.** ARCHITECTURE.md ingestion step 5 moved to `Implemented` and a step 6 was added; SECURITY.md moved four rows — "Thousands of files / deep nesting" and the two command-injection/code-execution rows to `Implemented`, "Source code in logs" to `Partial` — and updated three more without flipping them.

  Decisions worth carrying forward:
  - **The SHA is an out-parameter (`ArchiveRoot`), not a third tuple element or a result object.** It is one fact about the *archive*, so a third element would repeat a constant once per file and make all 142 existing archive tests unpack a value that never varies; and a generator cannot hand a return value to an ordinary `for` loop, so "return it at the end" was never available. The holder leaves the yield type alone and puts the awkward part in the type where a reader sees it: `str | None` until the first regular file is validated. That window is **inherent** — a streaming reader cannot know its root before reading a member — so no design avoids it. The pipeline never observes it, because it reads `sha` only on the path where at least one file was yielded, which is exactly when the root was checked.
  - **The SHA is *captured* by `ROOT_PATTERN`, not re-derived.** Adding `(?P<sha>…)` means the check and the harvest are the same match and cannot drift. A second rule would have to independently get `my-cool-repo-a1b2c3d` right; mutation-testing the obvious wrong version (split on the *first* hyphen) fails seven tests.
  - **The redirect's SHA hint is discarded, not used as a fallback.** `get_download_url` returns one only when the target pins a commit, which a branch ref does not. A fallback is exactly how a response ends up pinned to a commit the graph did not come from, so a test asserts the archive root wins even when the redirect *does* carry a SHA.
  - **`is_secret_path` runs before the parser, and a test pins the ordering rather than the outcome.** A spy on `extract_imports` records what was handed over; `.env.ts` — a real TypeScript file by extension — is asserted never to appear. Outcome-only assertions would still pass if the filter ran after the parse, which is precisely the arrangement ADR-012 cannot tolerate once file content reaches an LLM prompt.
  - **`skippedFiles` counts every yielded file that was not analyzed, including `README.md` and images.** Chosen over the narrower "files we refused" reading because it makes the number *checkable* rather than a judgement call: `len(files) + skipped_files` equals what the archive produced whenever truncation did not fire. `secret_files` is broken out separately, and it counts everything `is_secret_path` rejects — including `node_modules` and `dist`, so a vendored repository reports a large number there with no credential involved.
  - **A file the *parser* skipped is not a skipped file.** Binary, oversized, and pathological files still exist in the repository and still become nodes; they just contribute no edges. Counting them as skipped would have made the invariant above meaningless, and it is also unobservable from the pipeline — `extract_imports` yields nothing for a skipped file and nothing for a file with no imports.
  - **`MAX_SOURCE_FILES` is counted over accepted files, not archive members**, so a repository of assets is not truncated by its assets. Reaching it `break`s, which abandons the generator and the download; the cost is that no count of the remainder can be reported, and `truncated` is the honest substitute.
  - **A mid-transfer `httpx` failure had to be wrapped.** The archive reader catches `TarError`, `BadGzipFile`, `zlib.error`, and `EOFError` — all statements that *bytes arrived and were malformed*. A reset connection is a different statement and would have escaped as an untyped `httpx.ReadError`, breaking the error contract. `_guarded` converts it to `UPSTREAM_UNAVAILABLE`; deleting it fails a test.
  - **`.ts` versus TSX is pinned by a discriminating test, not by an assertion about which grammar object was chosen.** `const x = <string>y;\nimport './after';` yields its import under TypeScript and **nothing** under TSX, because TSX reads `<string>` as an unclosed JSX tag that swallows the rest of the file. Pointing `.ts` at TSX fails it.
  - **Nine controls were mutation-tested one at a time; all nine are caught** — the secret filter, the grammar split, the file cap, the download status check, the byte-stream guard, the SHA record, the SHA derivation, and two log-hygiene mutations (promoting a member path to `INFO`, logging extracted specifiers at `DEBUG`).
  - **`safe_relative_path` was deliberately not wired in.** Nothing in this path touches disk (ADR-003) and no resolved path exists yet — resolution is Task B. Calling it against a fabricated base to satisfy a checklist would be worse than leaving it uncalled and saying so.

- **2026-08-31** — **Wire contract extended for ADR-012**: `ServiceEndpoint` added to `app/models/api.py`, `serviceMap` and `c4` added to `AnalyzeResponse`, three new `Settings` limits, plus 17 tests (996 → 1013). This was the only place where existing code contradicted the sprint documentation; everything else the sprint added is unwritten work rather than divergence.

  Decisions worth carrying forward:
  - **Both narration fields default to absent**, and `ServiceEndpoint.summary` is optional beside them. ADR-012 calls LLM output a "display-only leaf value"; defaulting encodes that in the type, so a failed, disabled, or unconfigured LLM yields a valid response carrying the whole deterministic graph instead of a 500. Route detection and narration fail *independently* — unsummarized routes are a representable state, and the frontend can render the service map without ever seeing a summary.
  - **`method` is a character class (`^[A-Z]{1,16}$`), not a `Literal` of known verbs.** The detector is ours and may learn a router defining a method we did not enumerate; refusing to describe a route we successfully *found* is a worse failure than reporting an unusual verb. A test pins `GET\n` as rejected — Python's `re` lets `$` match before a trailing newline, and this asserts pydantic's Rust regex engine is stricter.
  - **The three new limits went to `Settings`, not to `Field(max_length=…)` literals**, and a test asserts they track the configured value rather than a hardcoded copy — the same discipline the URL and path bounds already follow. They bound what the LLM layer may put in a response body; a string is no less unbounded for having come from a model.
  - **`test_config.py` caught the omission before the suite went green.** It keeps an exhaustive table of every non-secret `Settings` field and asserts the table covers all of them, so three new limits failed the run until their defaults were documented. Worth knowing before adding a limit: the tripwire is deliberate.
  - **No `SECURITY.md` row moved.** The bounds are enforced at the model boundary but nothing produces these fields, and this project's own rule is that a rule nothing applies protects nothing — the same reason `secret_filter.py`'s row sits at `Partial`.

- **2026-08-29** — **Import extractor implemented**: `app/analysis/parser.py` plus 75 tests and two new `Settings` fields (`MAX_ERROR_NODE_CHILDREN`, `MAX_PARSE_TREE_VISITS`). `docs/SECURITY.md`'s "Parser crash or hang" and "Phantom dependencies" rows moved to `Implemented`, "Huge individual file" completed to `Implemented`, and a new row was added for the query-side hang. ADR-010 records why the planned timeout mechanism was abandoned.

  **The task brief's core mechanism does not exist.** It specified `parser.parse(source, progress_callback=lambda _s: deadline.expired())` and called it "the core timeout mechanism". Measured: with a `bytes` source tree-sitter ignores the callback entirely and warns; with the chunked-reader form it segfaults. Both are in the issues list above. Everything below follows from having to bound the cost structurally instead.

  Decisions and non-obvious behaviours worth carrying forward:
  - **The hang was never in the parser.** Parsing is roughly linear and cheap — the worst hostile 1 MiB input measured 3.1 s, most far less. The *query* is quadratic in the width of an ERROR node, which is why the guard inspects the tree rather than the clock. This inverted the whole design: `MAX_PARSE_BYTES` alone is **not** a sufficient bound, because 1 MiB is exactly the size at which the unguarded query costs eleven minutes.
  - **The guard keys on ERROR *width*, not on `has_error`.** Skipping every file with a syntax error would throw away the partial recovery that motivated tree-sitter in the first place. Real files sit orders of magnitude below the cap: the widest ERROR node in a file truncated mid-import is 4 children, while every hostile input is as wide as the file. A test pins both sides of that line.
  - **The `has_error` prune inside the walk is load-bearing, not an optimization** — this only surfaced under mutation testing. Without it, half a megabyte of ordinary code containing one syntax error visits ~280 000 nodes instead of ~40 000, trips `MAX_PARSE_TREE_VISITS`, and is silently dropped with all its imports. That is now its own test.
  - **`QueryCursor.captures()` cannot express the `require` filter and `matches()` can.** `captures()` returns `{"fn": [...], "src": [...]}` as two independently ordered lists with the match association discarded, so it cannot tell `require('./real')` from `describe('not an import')`. The brief specified `captures()`; using it would have meant either dropping every `require` or inventing a graph edge per test name — the exact phantom-dependency failure the AST approach exists to prevent.
  - **The compiled `Query` is cached per `Language`** (`lru_cache`). Compilation measures 8.8 ms, which at `MAX_SOURCE_FILES` would be ~26 s of the 60 s budget spent rebuilding a constant. `Parser` and `QueryCursor` are ~0.0005 ms and are built per call.
  - **`AnalysisTimeoutError` is the one exception the catch-all re-raises.** A timeout describes the run, not the file; swallowing it would turn an aborted analysis into a silently empty graph. Two deterministic tests pin that the deadline is consulted both before the parse and again between parse and query — the second matters because a slow parse can spend the remaining budget by itself.
  - **A NUL in a specifier never reaches the specifier check** — the binary sniff condemns the whole file first, which is the stronger reading. Other control characters cost only that one import. Both are pinned, because the ordering is otherwise incidental.
  - **26 controls were mutation-tested one at a time; 20 are caught.** The six survivors are annotated in the code: the BOM strip (both grammars already tolerate a BOM — verified for one, two, and mid-file), the `has_error` early-out (a pure fast path), the empty-`src` guard (every pattern binds `@src`, so a match cannot lack one), and the three string-literal checks in `_specifier` (the grammar only ever builds a `(string)` node for a *terminated* string, so matching quotes and a length of ≥2 are guaranteed — verified against unterminated, lone-quote, and mismatched-quote sources, none of which produce a `(string)` node at all).

- **2026-08-29** — **Archive reader: the missing Unicode-normalization tests added** (6 cases, `tests/test_archive.py`). An audit of the archive task against its brief found every validation layer and cap implemented and tested except this one, which had no counterpart in the suite. No production code changed — the reader was already correct — but "correct by accident" and "correct on purpose" are only distinguishable once a test pins it.

  The point is inverted from the other archive tests: normalization is asserted **absent**, not present. Under NFKC, U+FF0E FULLWIDTH FULL STOP folds to `.` and U+FF0F FULLWIDTH SOLIDUS folds to `/`, so normalizing would *manufacture* `../etc/passwd` out of a name that is one inert component in the bytes the archive actually carried. Both lookalikes are asserted to survive as a single component; NFC and NFD spellings of the same grapheme are asserted to round-trip as distinct code points, because a yielded path becomes a graph node ID and the subject of an `/api/source` token, and folding one spelling into the other would make the token miss the file the user clicked; and real ASCII `..` is asserted still rejected beside them. Confirmed by mutation — inserting `unicodedata.normalize("NFKC", name)` before the component check fails three of the six.

  **The residual risk is downstream, and it is worth carrying forward:** the guarantee is that *this module* compares exact code points. Anything later that normalizes a path — a normalizing filesystem, a `unicodedata.normalize` before a comparison, a database collation — undoes it. Recorded as its own row in `docs/SECURITY.md`.

  Two smaller things: the fullwidth characters are written as `\uff0e`/`\uff0f` escapes rather than literals, because they are visually identical to `.` and `/` — which is the entire premise of the attack and makes them unreadable in a diff — and this also satisfies ruff's `RUF001` honestly rather than with a `noqa`.

- **2026-08-29** — **Secret filter and path-safety guard implemented**: `app/security/secret_filter.py`, `app/security/path_safety.py`, plus 142 tests. `docs/SECURITY.md`'s "Future disk I/O reintroducing traversal" row moved to `Implemented`; the "Returning `.env`, keys, credentials" row moved to **`Partial`, not `Implemented`** — see below.

  Decisions and non-obvious behaviours worth carrying forward:
  - **The secret-exposure row is `Partial` on purpose.** The brief asked for `Implemented`, and the rule *is* implemented; but that row describes a filter "applied during analysis **and** re-applied independently in `/api/source`", and neither call site exists. SECURITY.md's own banner says a constant is not a control, and the last adversarial review caught a row falsely claiming `path_safety.py` was "implemented and tested" when the file did not exist. A rule nothing applies filters nothing. The row flips when both callers call it.
  - **`server.*` is deliberately narrower than the brief.** Read literally it blocks `server.ts` and `server.js` — the most common Node entry point there is — which would delete a real node from the graph, dangle every inbound edge, and 403 a file the user can already read on GitHub. The pattern is aimed at TLS material, so it matches `server.` plus a credential extension (`.crt .cert .csr .der .jks .keystore`); `.pem`, `.key`, and `.p12` were already covered by the global suffix rule, so nothing is lost. Confirmed with the requester before deviating.
  - **The allowed list is the load-bearing half of the spec.** Blocking secrets is easy; the failure mode that actually ships is a rule that eats source. `monkey.ts` and `keyboard.tsx` die to a substring search for `key`, `src/secrets.ts` dies if the exact-name rule becomes a prefix, `.gitignore` dies if the `.git` directory rule matches by prefix, and `src/build.ts` and a Bazel `BUILD` file die if the directory rules are applied to the final component instead of the parents only. Each is a test.
  - **Directory rules match parents; name rules match every component.** The asymmetry is the reason `BUILD` survives while `.env/keys.ts` does not — a *file* named `build` is source, but a *directory* named `.env` shields nothing.
  - **`casefold`, not `lower`.** Every difference between them widens the match, and the machine that produced the repository is frequently case-insensitive: `.ENV` and `ID_RSA` arrive intact and are exactly as sensitive.
  - **`commonpath`, not `str.startswith`** — `startswith` accepts `/srv/base-evil` as a child of `/srv/base`, since the prefix matches but the component boundary does not. And **the base is realpath'd too**: `/tmp` is `/private/tmp` on macOS and `tmp_path` inherits that, so resolving only the candidate would refuse every legitimate call. Both are covered by a test and by a mutation.
  - **An `if`, not an `assert`.** The brief said "assert the result stays inside base"; a real `assert` is stripped under `python -O`, which is exactly the deployment where the check still needs to hold.
  - `path_safety` raises a plain `ValueError` rather than an `AppError`: it is a low-level utility, not an HTTP boundary, and its caller owns the mapping. Its three messages are fixed literals — the NUL check exists partly so `os.stat`'s own `ValueError`, which quotes the path, never surfaces.
  - **The 12 controls in `secret_filter.py` and the 6 in `path_safety.py` were mutation-tested one deletion at a time; all 18 are caught**, including two *widening* mutations (directory rules applied to every component, and `commonpath` swapped for `startswith`). No survivors, so nothing needed a redundancy annotation.

- **2026-08-29** — **Streaming archive reader implemented**: `app/fetch/archive.py`, `app/analysis/deadline.py`, `tests/fixtures/tarballs.py`, plus 142 tests. `docs/SECURITY.md`'s "Path traversal and archive attacks" table moved to five `Implemented` rows, and four resource-exhaustion rows moved to `Implemented` or `Partial`.

  Decisions and non-obvious behaviours worth carrying forward:
  - **The gzip layer is ours, not `tarfile`'s** — `mode="r|"` over an explicit `gzip.GzipFile` rather than `r|gz`. This is the single most important design point in the module. `r|gz` leaves no seam between decompression and tar parsing, and metering has to happen at that seam: a non-seeking `tarfile` reads *past* the body of every member, including ones the reader skips for being oversized. A bomb whose payload is one 1 GiB member therefore yields **no files at all**, and any accounting that sums the sizes of accepted members sees zero bytes while a gigabyte goes through the decompressor. Metering the decompressed stream on every read kills it at ~8 MiB.
  - **A tar of pure zeros is not a bomb.** Confirmed empirically: `tarfile` reads the first zero block as the end-of-archive marker and stops, so a gzipped gigabyte of zeros yields zero members after ~10 KiB. The fixture had to become a tar *header* declaring a 1 GiB member followed by zeros. It is built by concatenating gzip members (`gzip` decodes a concatenated stream as one continuous output), so the 1 GiB bomb costs about a megabyte and no measurable time.
  - **The compression-ratio denominator is bytes *delivered to the decompressor*, not bytes pulled from the iterator.** Read-ahead sitting in the adapter's buffer would inflate the denominator and delay the trip. As a consequence the tests must chunk their input realistically — a whole archive handed over in one `read` makes every early-abort assertion vacuous — and any test that measures consumption must use incompressible filler, because a megabyte of `b"x"` arrives in the decompressor's first read.
  - **`Limits` is a no-default frozen view onto `Settings`.** It restates the field *names* but never the numbers, so `Settings` stays the only source of values (CLAUDE.md) while a test can still exercise one control with the others lifted out of the way.
  - **Rejecting the archive and skipping a member are different outcomes**, and the split is deliberate: a path an honest `git archive` could not have produced (absolute, traversing, multi-rooted, malformed) aborts the run; a path merely past a resource budget (too deep, too long, too big) drops that one file. Several tests exist only to prove a given input lands on the right side of that line.
  - **`RepositoryTooLargeError` for byte budgets, `ArchiveRejectedError` for structure.** No new `ErrorCode` was added — the 14 in `errors.py` are the frozen wire contract, and a bomb is adequately described by the existing archive-rejected code. The bomb test asserts the *ratio* error specifically, so it would notice if the 256 MiB extracted cap were quietly doing the work instead.
  - **The 24 controls in the module were mutation-tested one deletion at a time; 21 are caught.** The three survivors are annotated in the code: the absolute-path check is subsumed by the empty-component and root-name checks, and a negative member size is unreachable because `tarfile` raises `ReadError("invalid offset")` on such a header before the member is handed over. A fourth survivor was **not** redundant and was a real hole in the suite — deleting the `\` → `/` normalization left everything green, because every backslash case then in the tests failed on the root-name pattern instead. `root-sha/src\..\..\evil.ts` would have been yielded verbatim. Cases with backslashes *below* the root were added.
  - Two fixture-verifying tests exist on purpose: one asserts the pax fixture really produces a NUL in `member.name`, one asserts the surrogate fixture really writes a raw `\xff`. Without them, a future `tarfile` that sanitized either would turn both attack tests green for the wrong reason.

- **2026-08-29** — **GitHub client implemented**: `app/fetch/github.py` plus 88 tests, and three new `Settings` fields (`GITHUB_CONNECT_TIMEOUT_S`, `GITHUB_READ_TIMEOUT_S`, `MAX_GITHUB_CONNECTIONS` — timeouts are operational limits, so they live in `Settings` like every other one). This is the first module that can open a socket and the first caller of the egress guard. `docs/SECURITY.md` gained four `Implemented` rows (Arbitrary URL, Redirect to attacker host, Proxy env vars, Credential leak) plus the private-repo-oracle row, and two `Partial` ones (Huge repository, the two command-injection rows).

  **The assertion the net_guard task left owing is now written and passing**: both requests run through one client with a token configured, and the codeload request is asserted bare — under the header name, and by searching every header value for the token.

  Decisions and non-obvious behaviours worth carrying forward:
  - **The token is a per-request header, never a client default** (ADR-009). The brief suggested setting it on the client and deleting it before the download; that makes the credential's absence depend on a `del` a refactor can drop. With no client-level header there is nothing to inherit, so a request carries the token only if a call site names it — and the only one that does targets `api.github.com`. `download_request()` still pops an inherited header, for a client this module did not build.
  - **`get_download_url` returns `(url, sha | None)`, not `(url, sha)`.** GitHub redirects a *branch* ref to `.../legacy.tar.gz/refs/heads/main`, which pins no commit; only a SHA-shaped ref produces one. The authoritative SHA still comes from the tar root during extraction, as ARCHITECTURE.md always said. Returning a fabricated or empty string here would have quietly become a wrong commit pin in `/api/source`.
  - **A `Location` header is only honoured on a redirect status.** Mutation testing found this: deleting the status check left the whole suite green, because every non-redirect test case happened to omit `Location`. A `200 OK` carrying one would have been read as a download target. `304` is why the accepted set is enumerated rather than `300 <= status < 400`.
  - **`isinstance(value, bool)` before `isinstance(value, int)`**, because `bool` subclasses `int`: an unguarded integer check would accept `private: 0` and a size field of `True`.
  - **Percent-encoding a path segment is not validation.** `..` is entirely unreserved characters, so `quote("..", safe="")` returns `..` unchanged. Owner, repo, and every ref component are checked against a fixed character set *before* being encoded, and a hostile value is asserted never to reach the wire rather than merely to be encoded on the way out.
  - GitHub's canonical `name`/`owner.login` are re-validated on the way *in*. They are upstream data that gets interpolated into the tarball URL and later into node paths; the API being trustworthy today is not the same as the response being structurally constrained.
  - The **12 controls in the module were mutation-tested one deletion at a time; all 12 are caught** (11 on the first pass, plus the redirect-status check after its test was added).

- **2026-08-29** — **URL validation and the network guard implemented**: `app/security/url_validation.py`, `app/security/net_guard.py`, `tests/conftest.py`, plus 389 tests. `docs/SECURITY.md`'s Network/SSRF table moved from all-`Planned` to four `Implemented` rows and two `Partial` ones; the `Partial` halves are client behaviour (`follow_redirects=False`, the one-hop rule, no `Authorization` on the codeload request) and stay `Planned` until `app/fetch/` exists.

  Design decisions made here, none of which change the architecture:
  - **net_guard failures raise `UpstreamUnavailableError` (502), not a 4xx.** A refused redirect is a statement about GitHub's response, not about the URL the user submitted — that one already passed `parse_github_url`. Reusing the existing opaque upstream error also keeps the refusal from telling an attacker which check tripped.
  - **The user-facing grammar is strict where the egress guard is lenient**, deliberately: `parse_github_url` rejects `github.com.` (trailing dot), any port at all, and any query or fragment, while `validate_download_url` strips one trailing dot and permits `:443` and a query string, because codeload's real redirect targets carry a signed query.
  - `_FORBIDDEN_CHARS` is duplicated across the two modules rather than shared, so that tightening the user-facing grammar cannot silently alter what the egress guard accepts.

  Seven non-obvious behaviours were verified empirically and are recorded in code comments — each one is a bypass if you assume otherwise:
  - **`str.strip()` removes Unicode whitespace**, so the ASCII check must run *before* it. Stripping first would launder `"<NBSP>https://github.com/o/r"` into a clean ASCII URL and silently defeat the homograph defence. Mutation testing found this: deleting the `isascii()` call left every test passing until the NBSP/U+3000/U+2007 cases were added.
  - `urllib.parse.urlsplit` **silently deletes tab, CR, and LF from anywhere in a URL**. `urlsplit("https://gith\tub.com/o/r").hostname` is `'github.com'`. Both modules therefore screen bytes *before* parsing.
  - **`urlsplit` is not total.** It raises `ValueError` on a malformed bracketed host — `https://[evil.com]/o/r`, `https://[::1/x`, `https://[]/x` — and the message **quotes the offending host verbatim**. An adversarial review caught this escaping both functions as a bare `ValueError`, which broke the typed-error contract *and* the no-echo rule at once. Both modules now wrap `urlsplit`, and both test files carry a ~20 000-input fuzz sweep asserting nothing but the typed error escapes.
  - `ipaddress.ip_address(a).is_global` is **`True`** for `::127.0.0.1` (IPv4-compatible), `64:ff9b::7f00:1` (NAT64), `224.0.0.1` (multicast), **and every address in `fec0::/10`** (deprecated IPv6 site-local, RFC 3879 — CPython's `_reserved_networks` stops at `fe00::/9` and its `_private_networks` resumes at `fe80::/10`, leaving the block between them uncovered). `is_global` alone is *not* a public-address check. `assert_public_ip` also requires `not is_reserved`, `not is_multicast`, and `not is_site_local`; a sweep of all 65 536 IPv6 `/16` prefixes confirms those four predicates leave no further gap. The `is_site_local` test must be guarded by `isinstance(ip, IPv6Address)` — `IPv4Address` has no such attribute, so an unguarded access would raise `AttributeError` on every IPv4 address and escape the module untyped.
  - CPython 3.13+ `IPv6Address.is_global`/`is_reserved` **delegate to `.ipv4_mapped`** when set. That is the only reason `::ffff:140.82.121.4` is not caught by the `::/8` reserved check, and it is why the explicit unmapping must run *before* that check.
  - `ipaddress.ip_address()` **accepts an `int`**, so an unexpected `(int, bytes)` sockaddr from `getaddrinfo` would be silently read as a packed IPv4 address. Guarded by an explicit `isinstance(address, str)`.
  - `urlsplit(...).port` raises `ValueError` on a non-numeric port but returns `None` for an *empty* one, so `https://github.com:/o/r` slips past a port check. Caught by requiring the whole authority to equal the hostname.

  The suite was **mutation-tested**: 40 single-check mutations, 35 caught. The five survivors are checks that are redundant by design (the userinfo, port, and leading-slash checks in `url_validation` are subsumed by the authority-equality check; `.lower()` and the `::ffff:` unmapping restate stdlib behaviour). Each now carries a comment saying so, so a future reader does not mistake redundancy for an untested control.

  Two rounds of adversarial multi-lens review ran against this code. Round one found the `urlsplit` `ValueError` escape; round two found the `fec0::/10` gap and six stale or false documentation claims, including an `ARCHITECTURE.md` banner and a `SECURITY.md` row that described `path_safety.py` as "implemented and tested" when the module does not exist. Both defects were real, both are fixed, and the doc claims are corrected — worth recording because in both rounds the finding that mattered came from a lens that was told to *verify empirical claims by running them* rather than to read for style.

- **2026-08-29** — **Backend contract layer implemented**: `app/config.py`, `app/errors.py`, `app/models/{graph,api}.py`, `app/logging_setup.py`, plus 160 tests. The wire schema is now frozen, so frontend work can proceed against it in parallel. Scope was contract and plumbing only — no routes, no network, no parsing.
- **2026-08-29** — A multi-lens adversarial review of that contract layer confirmed 17 defects, all fixed and reverified. The four that mattered:
  - `Settings` inherited `extra="forbid"` from `BaseSettings`, so a single unrelated key in a shared `.env` aborted startup — and pydantic's `ValidationError` echoes the offending *value*, so a token under a near-miss name (`GH_TOKEN=ghp_…`) would print in cleartext via the default excepthook, which no logging filter can reach. Now `extra="ignore"`.
  - `models/api.py` hardcoded `300` and `1024` instead of reading `Settings`, so tightening `MAX_URL_LENGTH` had no effect at the boundary meant to enforce it. Bounds are now `AfterValidator`s reading `get_settings()`.
  - Redaction covered only `ghp_` and `github_pat_`. `ghs_` — what GitHub Actions puts in `$GITHUB_TOKEN`, and so the likeliest value an operator pastes into ours — passed through verbatim, as did `gho_`/`ghu_`/`ghr_`. The `Authorization` pattern also missed the `[('authorization', 'Bearer …')]` form that `httpx.Headers.items()` produces, which is exactly what one reaches for when debugging a 401 because httpx masks its own `repr`.
  - `RedactingFilter` scrubbed `record.exc_text`, but filters run *before* formatters, so that field is always `None` at filter time — tracebacks were redacted only by `JsonFormatter`, and a plain handler leaked them. The filter now renders the traceback itself.
- **2026-08-29** — Dependency versions verified live against PyPI and npm. Three traps recorded: R3F 10 is alpha and incompatible with drei 10 (pin R3F 9.7.0); TypeScript must be pinned to 5.9.3 because `typescript-eslint` 8.68 caps at `<6.1.0`; `d3-force-3d` ships no types and no `@types` package exists, so a local `.d.ts` is required. Also: `tree-sitter` 0.25/0.26 removed `Query.captures()` and `Language.query()` in favour of `QueryCursor`.
- **2026-08-29** — This documentation system created. Design rationale captured as ADR-001 … ADR-008 in [DECISIONS.md](DECISIONS.md), so the plan is now self-contained in the repository.
- **2026-08-29** — `.gitignore` added, `PRD.md` staged, `backend/` dependency scaffold created and installed. `pydantic-settings` pinned to **2.15.0** after checking PyPI; `ruff` **0.16.5** and `mypy` **2.3.1** pinned rather than floated, per the "pin dependency versions" rule. `hatchling` **1.32.0** added as the build backend — the only dependency beyond the agreed list, needed because a PEP 517 backend is required for the package to be installable at all.
- **2026-08-29** — tree-sitter ABI spike run for real: `Language(tree_sitter_typescript.language_tsx()).abi_version` → **`14`**. The `QueryCursor` API is present, confirming the 0.25/0.26 migration away from `Language.query()` / `Query.captures()` / `Query.matches()`.

## Next Steps

Full detail in [TODO.md](TODO.md). Summary, by sprint day.

**Day 1 (backend) — done, including the golden-file test.** Ingestion, resolution, the graph, descriptions, route detection, the component diagram, and the HTTP routes are all in and joined: `/api/analyze` returns a populated `componentDiagram`, `MAX_NODES`/`MAX_EDGES` are enforced, the error contract is returned, the request body is capped, and no limit in `Settings` is now unbound. **Done 2026-09-01**: `tests/test_api_routes.py` gained `test_the_whole_response_matches_the_golden_file`, comparing a pretty-printed `response.json()` against `tests/fixtures/analyze_response_golden.json` byte for byte, plus `test_the_same_commit_produces_byte_identical_json`, which calls `/api/analyze` twice against the same mocked repository and asserts the two bodies are identical — the first place ADR-013's "pure function of the commit" claim is pinned as a repeatability property rather than argued about. No Day 1 items remain open.

*(Historical note: this list previously called for the description extractor, route detection, the routes themselves, and the component-diagram generator. All four landed; the routes' `asyncio.wait_for` net was deliberately **not** built — see ADR-023 and Broken / Known Issues.)*

**Day 2 (frontend) — in progress.** Done: scaffold, 2D Cytoscape.js graph (ADR-022, superseded the R3F scene/sphere-packing layout), inspector panel (description is already in the response — no second call), landing page + URL input + loading state + the `POST /api/analyze` client, and both remaining panels — component diagram (`mermaid` 11.17.2, browser-verified, ADR-025) and service map. Remaining: **putting the two panels in the layout** (neither is imported, so neither is in the running app), the Cytoscape layout decision, collapse/expand, and search. *(The "first frontend test" that used to close this list landed 2026-09-01 — 35 tests over `ComponentDiagram.tsx` plus a repo-wide sink grep.)* ADR-013 removed this day's backend half; **the freed time is buffer**, not a slot to fill.

**Day 3 (deploy, ADR-011):** backend to Railway/Render/Fly, frontend to Vercel, CORS, README. Rate limiting and the concurrency gate landed early, off Day 3 (ADR-008; see Recently Completed) — the remaining resource control before a public deploy is CORS. The real-repo smoke check already exists (`backend/scripts/smoke.py`, ADR-014); what Day 3 still owes is **adversarial** coverage — a repository built to attack the guards, since the fixture suite is the only thing that has ever triggered one.

The tree-sitter spike is **fully done**: ABI 14, `QueryCursor` present, and `progress_callback` **unusable** (ADR-010). Nothing about tree-sitter remains unverified.

## Last Updated

2026-09-01
